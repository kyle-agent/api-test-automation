# Optional: AI features (triage, spec-impact, scenario drafts, fact extraction).
# Pulls httpx/httpcore transitively — if your network mirror blocks those, the
# platform still runs fully without this file; AI sections show 비활성.
anthropic>=0.40
