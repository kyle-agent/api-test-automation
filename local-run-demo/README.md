# Local Run — static demo

Backend-free snapshot built by `python -m controlplane.build_local_demo` (baked /api/local/* via a fetch monkeypatch; the real page JS is unchanged). Publish to `dashboard-data:/local-run-demo/`.
