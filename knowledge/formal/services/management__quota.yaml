# Layer 1 — per-service knowledge: management/quota
# Schema: knowledge/formal/FORMAT.md · validate: python knowledge/formal/validate.py
# Source: userguide /userguide/management/quota/overview/

version: 1
service: management/quota

constraints:
  - id: default-vs-applied-quota
    rule: "two quota values exist per service: Default Quota (플랫폼 초기값) and Applied Quota (사용자 증가 요청 -> 승인 시 적용); some quotas are adjustable, some fixed. THIS explains the VPC 5-vs-3 discrepancy: userguide '5' is the platform DEFAULT, our account enforces 3 (account-applied value). Verify the live value via the quota READ APIs (quota-reads lifecycle) before changing core/budgets limits"
    provenance: docs

captures: {}

states: {}

quirks:
  - id: no-prerequisites
    note: "선행 서비스 없음 (global service)"
    provenance: docs
