---
status: active
for: orchestrator
---

# SPEC-DIFF — 2026-09-17 (사이트 리디자인으로 표준 리프레시 실패, 대체 방법론으로 부분 diff)

> Spec-Intel 세션. 지시: "커밋된 카탈로그(2026-08-20 재스크랩, 커밋
> `68e793e6`)를 최신 라이브 SCP Open API 스펙과 비교해 뭐가 달라졌는지
> 정리". **결론 먼저: 사이트가 재구성돼 기존 `spec.extract_catalog`의
> discovery 단계가 통째로 깨져 있다** — index 페이지가 더 이상 전체
> 엔드포인트 href 목록을 담고 있지 않음(1417개 링크 → 사실상 1개).
> "unreachable"은 아니라서(모든 요청 200) §2 지시의 "stop" 조건에
> 문자 그대로는 안 걸리지만, 표준 파이프라인으로 정직한 전체 diff를
> 낼 수 없다는 점은 동일하게 심각한 차단 상황으로 판단해 **카탈로그
> 파일은 건드리지 않고 원복**했다. 대신 사이트가 새로 제공하는 서비스별
> "API Version History"(변경사항 changelog, `[신규]/[변경]/[삭제]`
> 태그)를 라이브에서 직접 읽어 63개 서비스 전체를 교차검증하는 대체
> 방법론으로 **부분적이지만 근거 있는 diff**를 만들었다. 라이브 SCP API
> 뮤테이션 없음(read-only GET만, docs.e.samsungsdscloud.com 대상).
> `data/` 산출물은 **전혀 수정하지 않음**(아래 §1 참고, 원복 완료 확인).

## 0. 헤드라인

| 축 | 수치 |
|---|---:|
| fetch 성공 여부 | **성공** — 프록시 통해 `docs.e.samsungsdscloud.com` 전 구간 200 (index, contents-list, 64개 서비스 overview 페이지, 구식 엔드포인트 페이지 샘플 전부) |
| **표준 파이프라인(`spec.extract_catalog`) 결과** | **깨짐** — index 페이지가 5MB(전체 nav 임베드) → 31KB(카테고리 링크만)로 축소됨. `--fresh` 재수집 시도 결과 discovered=1 (기존 1417 대비) — 사이트가 더 이상 per-endpoint href를 index에 안 실음 |
| 카탈로그 (`data/api_catalog.json`) | **변경 없음 — 1,417개 그대로 (커밋 상태로 원복 완료)**. `spec.summary`: resolved 1,416 / unresolved 1 (기존과 동일) |
| 대체 방법론으로 확인된 **서비스 삭제** | **1개**: `networking/cssdlan` — 서비스 개요 페이지 자체가 404 (기존 "미해결 1건"이 nav 오탈자가 아니라 **서비스 전체 폐기**였음을 이번에 확정) |
| 대체 방법론으로 확인된 **서비스 신설**(카탈로그 미포함) | **4개**: `application-service/messagehub`(29개 엔드포인트 전체 문서화됨, real diff), `financial-management/costnavigator` / `management/resourceoptimizer` / `security/cnapp`(3개는 Overview뿐인 스텁 페이지 — 엔드포인트 문서 아직 없음) |
| 대체 방법론으로 확인된 **버전업 서비스** (live `Version` 표 CURRENT > 카탈로그 보유 최고 버전) | **25개 / 60개** 기존 서비스. 그중 **19개는 changelog에 method+path 단위 상세**(합계 69개 태그 항목 — 신규 6 + 변경 63), **6개는 버전 숫자만 오르고 changelog 본문이 없거나 카탈로그 시점과 동일해 상세 불명**(iam, servicewatch, cdn, dns, gslb, sts — §2.3) |
| 변경 없음 확인된 서비스 | **34개** (60 − 1 삭제 − 25 버전업) |
| `data/spec_diff_latest.json` | **건드리지 않음** — 표준 `spec.diff` 실행 불가(구 카탈로그 vs "발견 1개"짜리 신 카탈로그는 무의미). 기존 파일(2026-08-20 시점 内용, added=1 `security/kms/exportpublickey`)이 최신 상태로 남음 |

**중요**: 위 "25개 버전업" / "34개 변경 없음"은 **`spec.diff`가 아니라
서비스 개요 페이지의 `Version` 표 + changelog를 63개 전부 직접 읽어
수작업으로 교차검증한 결과**다. 카탈로그의 엔드포인트별 body/response
필드 수준 변경(07-15 리포트가 했던 것과 같은 깊이)은 **이번 세션에서
검증하지 못함** — discovery가 깨진 상태에서 필드 단위 diff까지 하는 건
스코프 아웃으로 판단(§6).

## 1. 무엇을 했고, 왜 표준 파이프라인이 실패했는지

1. **사전 백업**: `data/api_catalog.json`, `api_bodies.json`,
   `api_catalog_params.json`, `spec_diff_latest.json`을 세션 scratchpad에
   백업. 리프레시 전 `spec.summary` 기준선: 1,417 discovered / 1,416
   resolved / 1 unresolved.
2. **`python -m spec.extract_catalog`(기본, resumable)** 실행 →
   `discovered 1417 endpoints; 1417 already in catalog` — 즉
   **전량 cache-hit로 no-op**(모듈 docstring이 명시한 알려진 한계: 기본
   모드는 변경 감지 불가). 카탈로그 파일 바이트 동일 확인(md5 일치).
3. `spec.extract_catalog`의 docstring에 따라 **변경 감지에는 `--fresh`가
   필수**라 재실행 → `data/.apiref_index.html`(2026-08-20 캐시, 5MB)를
   지우고 라이브 index를 새로 받게 함. 결과: **`discovered 1 endpoints;
   0 already in catalog`** — 카탈로그가 1개짜리로 통째로 덮어써짐(source
   of truth 훼손 위험). **즉시 scratchpad 백업으로 원복**하고
   `data/.apiref_index.html`도 `git checkout --`로 커밋 상태 원복(둘 다
   확인 완료, `git status --short data/` 클린).
4. **원인 조사(read-only curl 프로빙, 코드/데이터 변경 없음)**:
   - 라이브 `/apireference/` index 페이지: **31,054 bytes** (2회 재요청
     동일, 결정적/캐싱 아님). href는 13개 카테고리 링크 + 예시 엔드포인트
     1개(`ai-ml/cloud-ml/apis/clusterproduct/1.0`)뿐. 기존 모듈
     docstring이 서술한 "각 상세 페이지가 ~4.3MB, 전체 nav 트리를
     임베드"라는 전제 자체가 더 이상 안 맞음 — **사이트가 nav를 더 이상
     정적 HTML에 전량 임베드하지 않도록 재구성됨**.
   - `/apireference/contents-list/`(RSS `index.xml`이 가리키는 별도
     페이지)는 **카테고리→서비스 레벨**(63개 서비스) 링크를 담고 있음 —
     엔드포인트 레벨은 아님.
   - 서비스 overview 페이지(`/apireference/<cat>/<svc>/`)는 **개별
     엔드포인트 href 목록도 없음**(예: `compute/baremetal/` 페이지에
     `apis/*/​*` 링크 0개) — 대신 사람이 읽는 문서 형태로 `Version` 표
     (CURRENT/SUPPORTED/DEPRECATED)와 **"API Version History"라는
     changelog 섹션**(버전별 `[신규]/[변경]/[삭제]` 또는 영문
     `[New]/[Changed]/[Removed]` 태그 + `<code>METHOD /path</code>`)을
     담고 있음 — 이게 이번 세션이 활용한 대체 신호.
   - **구식 per-endpoint 페이지는 여전히 살아있음**: 카탈로그의 기존
     `doc_url`(예:
     `.../compute/baremetal/apis/listbaremetals/1.2/`)을 직접 재요청하면
     200 + 올바른 `<meta description>`(`get /v1/baremetals ...`)을 반환.
     즉 **카탈로그 자체가 틀린 건 아님** — discovery(신규/삭제 엔드포인트
     찾기)만 깨졌다.
   - 사이트 전역 검색 인덱스(`https://docs.e.samsungsdscloud.com/search-index.json`,
     86MB, minisearch 직렬화 포맷, `documentCount: 35779`)가 존재하긴
     하나 문서 저장소(제목/URL 배열)가 별도 구조라 이번 세션 예산 안에서
     안전하게 역공학하지 못함 — **후속 세션에서 스크레이퍼를 다시
     설계할 때 검토 대상으로 남김**(§6).
5. **대체 방법론 채택**: `data/api_catalog.json`의 63개 서비스(카탈로그
   60개 + `contents-list`에만 있는 4개)에 대해 각 overview 페이지를
   1회씩 GET(총 64 요청, 캐시/재시도 없이 전부 성공) → `Version` 표의
   CURRENT 버전을 카탈로그 보유 최고 버전과 비교 → 다르면 changelog에서
   그 사이 버전의 `[신규]/[변경]/[삭제]` 항목을 정규식으로 추출(태그 +
   HTTP 메서드 + path). 스크립트/원본 페이지는 세션 scratchpad에 보존
   (`svc_pages/*.html`, `svc_version_report.json`,
   `changelog_extract.json`) — 저장소 안에는 아무것도 쓰지 않음.

## 2. 상세 결과

### 2.1 삭제된 서비스 — `networking/cssdlan`
서비스 overview 페이지(`/apireference/networking/cssdlan/`) 자체가
`<title>404 Page not found</title>`. 카탈로그에는 원래 이 서비스의
엔드포인트가 **1개뿐이었고 그마저 `method`/`http_path`가 둘 다 null**
(`networking/cssdlan/setexternalcssdlannetworkchangeinfo`) — 07-09/07-15
리포트가 "nav에는 있는데 페이지가 404"라고 기록했던 바로 그 건이다.
이번에 서비스 페이지 자체가 사라진 걸 확인했으므로 **"nav 오탈자"가
아니라 서비스 전체가 폐기(deprecated/decommissioned)된 것으로 결론**.
카탈로그의 `unresolved: 1`은 이 건 하나이며, 향후 스크레이퍼를 고칠 때
이 서비스를 카탈로그에서 제거하는 게 맞다(이번 세션에서는 안 건드림).
**영향받는 시나리오**: 없음(애초에 method/path가 없어 어떤 lifecycle도
이 엔드포인트를 참조할 수 없었음).

### 2.2 신설된 서비스 (카탈로그 미포함, 4개)

| 서비스 | 상태 | 문서화된 엔드포인트 |
|---|---|---|
| `application-service/messagehub` | **완전 문서화됨** | **29개** (push-applications ×7, phones ×8, emails ×8, domains ×5, 확인된 목록은 전부 `[New]` 태그 — CRUD형 알림 채널 관리 API로 추정) |
| `financial-management/costnavigator` | 스텁 | 0 — Overview + Version(1.0 CURRENT)만 있고 엔드포인트/changelog 본문 없음 |
| `management/resourceoptimizer` | 스텁 | 0 — 동일 |
| `security/cnapp` | 스텁 | 0 — 동일 |

`messagehub`는 실제 신규 커버리지 기회(29개 엔드포인트, method 상세는
scratchpad `new_services_endpoints.txt` 참고). 나머지 3개는 Samsung이
서비스명을 nav에 올려놨지만 API 문서 본문을 아직 안 채운 상태로 보임 —
**지금 카탈로그화 시도해도 의미 있는 엔드포인트를 못 뽑음**, 재확인은
차기 세션으로 미룸.

### 2.3 버전업된 기존 서비스 — method+path 상세 확보된 19개

카탈로그 보유 최고 버전 → 라이브 `Version` 표 CURRENT, changelog에서
뽑은 `[신규]/[변경]` 항목(중복 제거, 세부 문구는 생략, 전체는 scratchpad
`changelog_summary.txt` 참고):

| 서비스 | 버전 | 변경 항목 수 | 요약 |
|---|---|---:|---|
| application-service/queueservice | 1.2→1.3 | 6 변경 | GET/POST/PUT 큐 조회·생성·설정 계열 |
| compute/scf | 1.4→1.5 | 2 변경 | 환경변수 조회/수정에 `encryption.*` 필드 |
| compute/virtualserver | 1.4→1.5 | 4 변경 | 서버 생성/목록/상세/수정 |
| container/ske | 1.5→1.6 | 5 변경 | 클러스터 생성/조회, 이미지, 노드풀 목록 |
| data-analytics/searchengine | 1.1→1.2 | 2 변경 | 백업 이력/백업 삭제 |
| data-analytics/vertica | 1.1→1.2 | 2 변경 | 백업 이력/백업 삭제 (searchengine과 동일 패턴) |
| database/cachestore | 1.1→1.2 | 2 변경 | 백업 이력/백업 삭제 |
| database/epas | 1.2→1.3 | 2 변경 | 백업 이력/백업 삭제 |
| database/mariadb | 1.2→1.3 | 2 변경 | 백업 이력/백업 삭제 |
| database/mysql | 1.2→1.3 | 2 변경 | 백업 이력/백업 삭제 |
| database/postgresql | 1.2→1.3 | 2 변경 | 백업 이력/백업 삭제 |
| database/sqlserver | 1.1→1.2 | 2 변경 | 백업 이력/백업 삭제 |
| financial-management/budget | 1.0→1.1 | 2 신규 + 4 변경 | `cost-linkage` 신규 PUT/DELETE, 계정 예산 CRUD 변경 |
| management/iam-identity-center | 1.5→1.6 | 7 변경 | account-assignments, groups CRUD, users 추가 |
| management/organization | 1.2→1.3 | 3 신규 + 1 변경 | `delegation-accounts` 신규 GET/POST/DELETE |
| networking/firewall | 1.1→1.2 | 2 변경 | 방화벽 규칙 생성/수정 |
| networking/vpc | 1.3→1.4 | 1 신규 + 9 변경 | internet-gateway, privatelink-endpoint CRUD 변경 + `privatelink-services/scp-services` 신규 GET |
| storage/archivestorage | 1.2→1.3 | 7 변경 | archiving-policy CRUD, recover/cancel 계열 |
| storage/backup | 1.3→1.4 | 2 변경 | backup 삭제, restore-target 삭제 |

**DBaaS 6개 엔진(cachestore/epas/mariadb/mysql/postgresql/sqlserver) +
searchengine/vertica 총 8개가 동일 패턴**(`PUT .../backup-histories`,
`DELETE .../backups` 변경) — 07-15 리포트가 다룬 "9개 엔진 공통 웨이브"
스타일의 또 다른 플랫폼 전역 변경으로 보임(가설, 필드 수준 확인 안 함).

### 2.4 버전업됐지만 changelog 상세 불명 — 6개 (재검증 필요)

| 서비스 | 카탈로그 최고 | live CURRENT | 비고 |
|---|---|---|---|
| management/iam | 1.4 | 1.5 | changelog 최신 항목이 **1.4까지만** 문서화됨(1.5 항목 없음) — 문서 게시 지연으로 추정, 실제 계약 변경 여부 불명 |
| management/servicewatch | 1.4 | 1.5 | 페이지에 **"API Version History" 섹션 자체가 없음**(Overview만 존재) — 문서화 누락 |
| networking/cdn | 1.0 | 1.1 | changelog 1.1 항목이 "CDN API가 1.1 버전으로 업데이트 되었습니다"뿐, method/path 없는 포괄 문구 |
| networking/dns | 1.3 | 1.4 | 1.4 항목이 "Hosted Zone/Public Domain API가 1.4로 업데이트"뿐, 상세 없음 |
| networking/gslb | 1.1 | 1.2 | 1.2 항목이 "생성 API가 변경(env_usage=PRIVATE 미지원)" 정도만, 특정 엔드포인트 태그 없음 — 주의: PRIVATE 미지원은 **breaking 가능성 있는 문구**라 별도 플래그 |
| platform/sts | 1.1 | 1.3 | changelog가 **1.2까지만** 문서화(1.2 항목도 "1.1 버전에서 변경사항 없음"), 1.3 항목 자체가 없음 |

이 6개는 "버전 번호는 올랐다"는 것만 확실하고 실제 필드 수준 변경 여부는
문서만으로는 결론 못 냄 — **회귀 담당은 스모크/conformance보다 먼저
라이브 재요청으로 직접 확인 권장**(특히 gslb의 `env_usage=PRIVATE`
문구).

### 2.5 변경 없음 확인 — 34개
카탈로그 최고 버전 == live CURRENT: ai-ml/{aimlops-platform,cloud-ml},
application-service/apigateway, compute/{baremetal,multinodegpucluster},
container/scr, data-analytics/{data-flow,data-ops,eventstreams,
quick-query}, devops-tools/devopsservice,
financial-management/{billingplan,costexplorer,pricing},
management/{cloudcontrol,cloudmonitoring,loggingaudit,network-logging,
quota,resourcemanager,support}, networking/{direct-connect,
loadbalancer,security-group,vpn}, platform/product,
security/{certificatemanager,configinspection,kms,secretsmanager,
secretvault}, storage/{baremetal-blockstorage,filestorage,
parallel-filestorage}. (전체 목록/원본 페이지는 scratchpad
`svc_version_report.json` 참고.)

## 3. 재테스트 대상 서비스 (오너/회귀 담당 handoff)

**확정 변경(§2.3, method+path 상세 있음) — 19개, 우선순위 상**:
application-service/queueservice · compute/scf · compute/virtualserver ·
container/ske · data-analytics/searchengine · data-analytics/vertica ·
database/cachestore · database/epas · database/mariadb ·
database/mysql · database/postgresql · database/sqlserver ·
financial-management/budget · management/iam-identity-center ·
management/organization · networking/firewall · networking/vpc ·
storage/archivestorage · storage/backup

**신규 커버리지 기회 — 1개**: application-service/messagehub(29개
엔드포인트, 카탈로그에 전혀 없음 — coverage_gap 담당에게 별도 전달 필요)

**서비스 폐기 확인 — 1개**: networking/cssdlan (재테스트 불필요, 카탈로그
정리 대상으로만 표시)

**버전업 확인, 상세 불명이라 라이브 재검증 먼저 필요 — 6개**:
management/iam · management/servicewatch · networking/cdn ·
networking/dns · networking/gslb(특히 `env_usage=PRIVATE` 문구) ·
platform/sts

**스텁이라 아직 액션 불가 — 3개**: financial-management/costnavigator ·
management/resourceoptimizer · security/cnapp (다음 세션에서 문서가
채워졌는지만 재확인)

## 4. 아티팩트 변경 여부

**`data/` 디렉터리: 아무것도 커밋 대상으로 변경하지 않음.**

- `data/api_catalog.json` — `--fresh` 시도로 1개짜리로 훼손됐다가
  scratchpad 백업으로 즉시 원복, md5 일치 확인 완료. **1,417개/1,416
  resolved/1 unresolved 그대로.**
- `data/.apiref_index.html` — `--fresh` 과정에서 지웠다가 `git checkout
  --`로 커밋 상태 원복.
- `data/api_bodies.json`, `data/api_catalog_params.json` — 손대지 않음
  (byte-identical, diff 없음 확인).
- `data/spec_diff_latest.json` — **갱신 안 함**. 표준 `spec.diff`를
  의미 있게 돌릴 수 없었기 때문(구 카탈로그 vs "발견 1개" 카탈로그는
  diff 자체가 무의미 — fabricate 금지 가드레일에 따라 실행 안 함). 파일은
  2026-08-20 시점 내용(added=1: `security/kms/exportpublickey`) 그대로
  남아 있음 — **이번 세션에서 발견한 25개 서비스 버전업/4개 신설/1개
  삭제는 이 JSON에 반영돼 있지 않으니 참고 시 주의**.
- `python -m spec.summary` 최종 재확인: `Total endpoints discovered :
  1417`, `Resolved : 1416`, `Unresolved : 1` (가드레일대로 0에 가깝게
  유지, cssdlan 1건 그대로 — §2.1에서 원인 확정).
- `git status --short data/ docs/working/`: 이 리포트 파일 추가 외
  깨끗함(세션 종료 시점 기준).

## 5. 미해결 질문 / 후속 권장 (수행은 안 함, hand-off만)

1. **스크레이퍼 재설계가 필요함**: `spec/extract_catalog.py`의 discovery
   로직(index 페이지 href 정규식)이 새 사이트 구조와 안 맞는다. 두 가지
   후보 경로: (a) `contents-list` → 서비스 개요 페이지 → **구식
   `/apis/<name>/<version>/` URL을 서비스 페이지에서 직접 링크하지
   않으므로**, 엔드포인트별 URL을 알아내려면 changelog의 method+path를
   `<name>` slug로 역매핑하는 휴리스틱이 필요(불안정), 또는 (b)
   `https://docs.e.samsungsdscloud.com/search-index.json`(86MB minisearch
   인덱스, `documentCount: 35779`)을 파싱해 `apireference` 섹션 문서
   전체 URL 목록을 뽑아내는 방법 — 이쪽이 더 신뢰할 만해 보이지만 이번
   세션 예산 안에서 포맷을 다 풀지 못함(문서 저장소 배열 위치 미확인).
   **다음 spec-intel 세션의 최우선 작업으로 남김.**
2. **구식 per-endpoint 페이지(예: `.../apis/listbaremetals/1.2/`)가
   당분간 살아있는지는 불확실** — 오늘은 200이었지만 사이트가 리디자인
   중이라는 것 자체가 곧 폐기될 신호일 수 있음. 카탈로그가 갑자기 전부
   404가 나기 시작하면 이 리포트를 참고할 것.
3. §2.4의 6개 서비스(특히 gslb `env_usage=PRIVATE` 문구)는 changelog가
   불완전해서 판단을 못 내렸다 — **라이브 API에 직접 GET/생성 시도해
   실제 계약을 확인**하는 게 changelog 재크롤보다 빠를 수 있음(회귀
   담당 판단).
4. 07-15 리포트가 남긴 "cssdlan은 우리 쪽 fetch 문제 아님, nav 오탈자로
   추정"이라는 결론을 이번에 "서비스 전체 폐기"로 **정정**한다 —
   차기 카탈로그 정리 시 이 1개 엔트리(`unresolved: 1`)를 제거해도
   안전함(참조하는 lifecycle 없음, §2.1).
5. `financial-management/costnavigator` /
   `management/resourceoptimizer` / `security/cnapp` 스텁 3개는 문서가
   채워지는 대로(다음 세션 재확인) 카탈로그·coverage_gap에 반영 필요.

## 6. 방법론 자가평가

이번 세션은 "unreachable → stop, fabricate 금지" 가드레일의 정신을
**"discovery가 깨져서 표준 diff를 못 낸다"는 상황에도 동일하게
적용**했다 — 카탈로그를 함부로 갱신하거나 `spec_diff_latest.json`을
가짜로 채우지 않고, 대신 사이트가 실제로 제공하는 별도의 신뢰 가능한
신호(서비스별 Version 표 + changelog)를 찾아 **부분적이지만 근거를
댈 수 있는 diff**를 만드는 쪽을 택했다. 63개 서비스 전수 조사이므로
"파급이 큰 것부터 몇 개 샘플링"보다는 커버리지가 넓지만, 07-15
리포트가 했던 **필드 단위(body/response) diff는 하지 못했다** — 그건
구식 페이지가 계속 살아있다는 전제(§5-2) 위에서만 가능하고, 그 전제
자체가 이번에 흔들렸기 때문에 다음 세션에서 스크레이퍼를 고친 뒤 다시
시도해야 한다.
