---
status: active
for: orchestrator
---

# SPEC-DIFF — 전체 명세 리프레시 + diff (2026-07-15, 버전업 대응)

> Spec-Intel 세션. 오너 지시: "버전업이 되어서 최신 전체 명세를 가져오고 뭐가
> 달라졌는지 식별" — 라이브에서 이미 확인된 subnet 케이스(`type` enum
> GENERAL→PUBLIC/PRIVATE/LOCAL/VPC_ENDPOINT, `category` 신설 필수 필드)를
> 자가검증 앵커로 삼아 카탈로그(1,372→1,416개) + 요청 바디 스키마를 리프레시하고
> diff했다. **라이브 SCP API 뮤테이션 없음** — `docs.e.samsungsdscloud.com`
> fetch만 수행 (read-only, guardrail 준수). knowledge/·regression/ 파일은
> **수정하지 않음** (식별만, 지시대로).

## 0. 헤드라인

| 축 | 수치 |
|---|---:|
| 카탈로그 엔드포인트 | **1,372 → 1,416** (+45 신규, −1 삭제) |
| 카탈로그 시그니처(method/http_path/title/version/doc_url) 변경 | **932개** (아래 §1.3 참고 — 대부분 페이지 버전 태그만 증가, 실제 컨텐츠 불변으로 보임) |
| 요청 바디 스키마가 실제로 바뀐 것으로 확인된 엔드포인트 | **40개** (모델 페이지가 새 `vN.M` 버전으로 분기된 create-계열 write 엔드포인트) |
| 그중 **신규/개명 필수(required) 필드**가 생겨 **breaking**인 것 | **12개 (method,path,service) 조합 / 20개 엔드포인트 키** (zone 4개 서비스 공유 경로 포함) |
| breaking 변경이 기존 시나리오/lifecycle 스텝을 실제로 건드리는 곳 | **57개 스텝**, 그중 **48개가 아직 미수정**(400 위험), 9개는 이미 수정됨(subnet PRIMARY 8곳 + sqlserver 1곳) |
| 영향받는 서비스(재테스트 대상) | **11개**: compute/virtualserver · networking/vpc · security/configinspection · storage/baremetal-blockstorage · storage/filestorage · storage/parallel-filestorage · data-analytics/searchengine · data-analytics/vertica · database/cachestore · database/epas · database/postgresql |
| 카탈로그 미해결(unresolved) | **1개** — `networking/cssdlan/setexternalcssdlannetworkchangeinfo`: nav에는 있으나 페이지 자체가 라이브에서 `404 Page not found` (문서 사이트 자체의 깨진 링크, 재요청 3/3 동일 — 우리 쪽 fetch 문제 아님) |

**방법론 자가검증(subnet 케이스): 성공.** subnet은 (a) 카탈로그 시그니처
레벨에서도 잡힘 — `createsubnet` 엔드포인트 자체의 문서 버전이 `1.2→1.3`으로
올라갔다(도구/07-09 리포트가 우려했던 "바디만 바뀌고 엔드포인트 버전은 안
바뀔 수 있다"는 케이스가 아니었음), (b) 바디 스키마 레벨에서도 잡힘 —
`SubnetCreateRequestV1Dot2`→`V1Dot3`, `type` enum
`(GENERAL,LOCAL,VPC_ENDPOINT)`→`(PUBLIC,PRIVATE,LOCAL,VPC_ENDPOINT)`,
신규 필드 `category`(필수, enum PRIMARY/SECONDARY) + `primary_subnet_id`(옵션).
카탈로그 시그니처만 보는 07-09식 diff와 바디 스키마까지 보는 이번 diff 둘 다
동일하게 subnet을 "변경됨"으로 잡아냈다 — 방법론이 유효함을 확인.

## 1. 방법론

1. **사전 백업**: 리프레시 전 `data/api_catalog.json`, `api_bodies.json`,
   `api_docs.json`, `api_catalog_params.json`을 세션 scratchpad에 백업
   (`.../scratchpad/backup-20260715/*.old.json`, git HEAD와 바이트 동일 확인).
2. **카탈로그 전체 강제 리프레시**: `python -m spec.extract_catalog --fresh`
   (인덱스 캐시 + 1,372개 상세 페이지 전부 무시하고 재수집). 1,416개 엔드포인트
   발견, 1,415개 method/path 해석 성공, 1개 미해결(§0). 소요 약 40분.
3. **`python -m spec.diff <old> <new>`** — 카탈로그 시그니처(method/http_path/
   title/version/doc_url) 레벨 diff: +45/-1/~932/439 unchanged.
4. **바디 스키마 레벨 diff (이번 세션의 핵심 확장)**: 카탈로그 시그니처만으로는
   "바디 필드가 바뀌었는지"를 못 잡는다(07-09 리포트가 명시한 한계). 그래서:
   - `data/.apiref_index.html`(리프레시로 이미 최신)에서 모델 링크 전체를
     재파싱(`spec.scrape_docs.discover_models()`) → 라이브 모델 페이지
     2,531개 vs 구 스냅샷 2,306개 = **271개 모델 페이지 신규, 46개 소실**.
   - 구 스냅샷의 write 엔드포인트(POST/PUT/PATCH) 636개가 참조하는 바디
     schema_ref 556개를 베이스명(`v\d+dot\d+` 접미사 제거)으로 정규화하고,
     신규 모델 271개 중 같은 베이스명·더 높은 버전을 가진 것을 교차매칭 →
     **40개 write 엔드포인트가 실제로 상위 버전 바디 모델을 갖게 됨** 후보 확정.
   - 이 40개 전부에 대해 **엔드포인트 자체의 문서 버전도 함께 올랐는지**
     교차검증 → **40/40 일치**(예: `createsubnet 1.2→1.3` ↔
     `SubnetCreateRequestV1Dot2→V1Dot3`). 이 상관관계가 매우 강해
     "932개 시그니처 변경 중 어느 것이 진짜 바디 변경을 동반하는지"를
     시그니처만으로도 상당히 예측 가능함을 시사(§1.3 참고).
   - 40개 신규 모델 페이지를 라이브에서 개별 fetch, 구 모델(있는 경우)과
     필드 단위(name/required/schema) diff → **31개 모델에서 필드 추가/삭제/
     required 플래그 변경 발견**, 나머지 9개는 페이지는 새로 생겼지만 필드
     델타 없음(문서 정리성 변경으로 추정, 아래 §7 미해결 질문).
5. **데이터 반영**: `spec.extract_bodies` / `spec.scrape_docs`의 내부 함수를
   그대로 사용해 이 40개 엔드포인트 + 40개 모델을 정식으로
   `data/api_bodies.json`(20개 신규 캐시 + 20개 stale 교체 → 192개) /
   `data/api_docs.json`(endpoints 40개 갱신, models 2,306→2,346)에 병합.
   `data/api_catalog.json`은 1,416개로 전량 교체. `data/api_catalog_params.json`은
   **이번 세션에서 리프레시하지 않음**(쿼리 파라미터 카탈로그, 별도 도구
   `spec.enrich_catalog` 소관 — 스코프 아웃, §8).
6. **시나리오/야믈 영향 식별** (수정은 안 함): `regression/scenarios/scenarios.json`
   + `lifecycles/*.json` 전체를 파싱해 breaking 조합의 (method,path,service)와
   매칭, 스텝의 `json` 바디에 이미 새 필드가 있는지(=이미 수정됐는지) 확인.
   `knowledge/formal/resources/*.yaml`도 grep으로 대조.

### 1.3 "932개 변경"의 실제 의미 — 표본 검증

카탈로그 시그니처 diff가 잡은 932개 필드 변경은 전부 `{title, version, doc_url}`
세 필드뿐(= 문서 페이지의 버전 태그가 올랐다는 뜻; 이 스키마에서 `title`은
API 이름이 아니라 페이지 버전 문자열 그 자체라 `version`과 사실상 동일 신호).
**`method`/`http_path` 변경은 0건** — 라우트 자체가 바뀐 곳은 없다.

표본 검증: `compute/baremetal/listbaremetals` (GET, 1.1→1.2, 932건 중 하나,
바디 스키마 매칭 대상 아님)을 라이브 재요청 → 쿼리 파라미터 목록
(`size,page,sort,server_name,state,ip,vpc_id`)이 구 스냅샷과 **완전히 동일**.
즉 이 엔드포인트는 페이지 버전만 올랐지 계약(contract)은 안 바뀐 것으로
보인다 — 아마 같은 서비스의 write 엔드포인트가 바뀌면서 서비스 전체 문서
번들이 함께 재배포된 부수효과로 추정(가설, 미검증). **따라서 "932개가 전부
API 변경"이라고 읽으면 과대추정**이다. 이번 세션은 write(POST/PUT/PATCH)
바디 스키마 변경만 정밀 검증했고(§1.4), GET 전용/응답 스키마만 바뀌었을
가능성은 스코프 밖(§8 미해결 질문).

## 2. 파급이 큰 변경 — 상세 (영향 큰 순)

### ① Zone(가용영역) 신설 필수 필드 — 플랫폼 전역 8개 create 엔드포인트
`zone` (string, **required**)이 새로 생김. 대응하는 조회용 신규 엔드포인트
`storage/filestorage/listvolumereplicationzones` (`GET /v1/replications/zones`)도
같은 웨이브에서 함께 추가됨 — 멀티-AZ 기능 출시로 판단.

| 엔드포인트 | 경로 | 현재 시나리오 상태 |
|---|---|---|
| compute/virtualserver/createvirtualserver | POST /v1/servers | **미수정** (3 스텝) |
| compute/virtualserver/createvolume | POST /v1/volumes | **미수정** (4 스텝) |
| networking/vpc/createpublicip | POST /v1/publicips | **미수정** (5 스텝) |
| storage/baremetal-blockstorage/createvolume | POST /v1/volumes | **미수정** (2 스텝) |
| storage/baremetal-blockstorage/createvolumegroup | POST /v1/volume-groups | **미수정** (1 스텝) |
| storage/baremetal-blockstorage/createvolumegroupreplication | POST /v1/volume-groups/{id}/replications | **미수정** (1 스텝) |
| storage/baremetal-blockstorage/createvolumereplication | POST /v1/volumes/{id}/replications | **미수정** (1 스텝) |
| storage/filestorage/createvolume | POST /v1/volumes | **미수정** (3 스텝; `region` 필드는 제거됨) |
| storage/filestorage/createvolumereplication | POST /v1/replications | **미수정** (1 스텝; `region` 필드 제거됨) |
| storage/parallel-filestorage/createvolume | POST /v1/volumes | **미수정** (1 스텝) |

`compute/virtualserver/createautoscalinggroup`(POST /v1/auto-scaling-groups)도
같은 웨이브: **`placement_strategy`(required) + `zones`(required, 배열)** 신설 —
단일 `zone`이 아니라 복수 `zones` 형태. 3 스텝 미수정.

### ② DBaaS `service_watch_log_collection` 신설 필수 필드 — 6개 엔진
`POST /v1/clusters`(create-cluster) 바디에 `service_watch_log_collection`
(boolean, **required**)이 신설. **mysql/mariadb는 이미 이 필드를 요구하고
있었다** (모델 페이지 버전 안 오름, v1dot1 그대로 — 이번 웨이브의 대상이
아니라 이미 존재하던 요구사항이었고, 해당 두 서비스 시나리오는 이미 필드를
보냄, 정상). 이번에 **새로** 요구되는 6개 엔진:

| 엔진 | 현재 시나리오 상태 |
|---|---|
| database/postgresql | **미수정** (7 스텝: `subops-full.json` a/b/full + `scenarios.json` + `generated__heavy-pg.json` 3곳) |
| database/epas | **미수정** (6 스텝: `subops-full.json` 3 + `generated__heavy-dbaas.json` 3) |
| database/cachestore | **미수정** (3 스텝, `subops-full.json`) |
| data-analytics/searchengine | **미수정** (1 스텝, `create-cluster-waiver`) |
| data-analytics/vertica | **미수정** (1 스텝, `create-cluster-waiver`) |
| database/sqlserver | ✅ 이미 수정됨 (`sqlserver-cluster-subops-guarded`의 `create-cluster-guarded` 스텝이 이미 필드를 보냄) |

### ③ Subnet `category` 필수 필드 + `type` enum 확장 — 대부분 이미 수정됨
`networking/vpc/createsubnet` (POST /v1/subnets): `type` enum
`GENERAL→(PUBLIC,PRIVATE,LOCAL,VPC_ENDPOINT)`, **신규 필수 `category`**
(enum PRIMARY/SECONDARY), 신규 옵션 `primary_subnet_id`(SECONDARY 서브넷이
PRIMARY를 참조하는 용도로 추정). 커밋 `736fc587`/`8641e884`가 오늘 이미
type=PUBLIC + category=PRIMARY로 8개 라이프사이클 52곳을 패치함.

**미해결로 남아있던 부분을 이번에 확정**: 커밋 메시지가 "endpoint-subnet
(type=VPC_ENDPOINT)에도 category가 필수인지 미상, 일부러 안 건드림"이라고
남겨뒀던 질문 — **라이브 모델 스펙 확인 결과 `category`는 subnet 타입과
무관하게 무조건 필수**(스키마에 조건부 표시 없음). 즉 아래 3개 VPC_ENDPOINT
서브넷 생성 스텝도 400 위험:
- `regression/scenarios/lifecycles/generated__light-batch2.json` → `gen-vpc-endpoint` / `create-endpoint-subnet`
- `regression/scenarios/lifecycles/generated__wave5-net.json` → `gen-wave5-vpce` / `create-endpoint-subnet`
- `regression/scenarios/lifecycles/networking__vpc.json` → `vpc-endpoint` / `create-subnet`

(`heavy-shared-dbaas`의 `create-subnet` 스텝은 `enabled:false`라 당장 실행
안 됨 — 별도로 8곳에 포함되어 카운트됨.)

### ④ NAT Gateway `publicip_id` → `publicip_ids` 필드명 변경(breaking rename)
`networking/vpc/createnatgateway` (POST /v1/nat-gateways): 단수 `publicip_id`
(string) 필드가 **제거**되고 복수 `publicip_ids`(array, **required**)로
대체 + 신규 옵션 `multi_zone_enabled`. `scenarios.json`의
`heavy-shared-networking`/`natgw-create` 스텝(`optional:true`)이 여전히 구
필드 `publicip_id`를 보내는 중 — **미수정**.
`knowledge/formal/resources/networking__vpc.yaml:155`도 동일하게 구 필드
사용 중(수정 안 함, 식별만).

### ⑤ ConfigInspection 진단 바디 필드 교체
`security/configinspection/creatediagnosisobject`
(POST /v1/configinspection/diagnosis/save): 신규 필수
`check_list_version_id` — 대응하는 신규 조회 엔드포인트
`security/configinspection/getchecklistversion`
(`GET /v1/configinspection/diagnosis/checklist/version`)도 같은 웨이브에서
추가됨 (체크리스트 버전을 먼저 조회해서 넣으라는 설계로 추정).
`security/configinspection/diagnosisrequest`
(POST /v1/configinspection/diagnosis/request): 기존 `diagnosis_check_type`
필드가 **제거**됨(대체 필드 없음, 단순화로 추정).
`security__configinspection.json`의 `save`/`request` 스텝 둘 다 미수정
(save는 신규 필수 필드 누락 위험, request는 이제 존재하지 않는 필드를 여전히
보내는 중이나 보통 미지 필드는 무시되므로 상대적으로 저위험).

### ⑥ 신규 리소스: Address Group (networking/security-group, 8개 신규 엔드포인트)
CIDR 목록을 이름으로 그룹핑해 재사용하는 신규 리소스
(`POST/GET/PUT/DELETE /v1/address-groups`, `.../addresses`,
`.../add_addresses`, `.../remove_addresses`). 기존
`networking/security-group/createsecuritygrouprule`
(POST /v1/security-group-rules)에도 신규 옵션 필드
**`remote_address_group_id`**가 생겨 규칙의 remote target으로 address group을
직접 참조 가능해짐(옵션이라 breaking 아님, 커버리지 기회).

### ⑦ 신규 리소스: SQL Server DR Secondary (database/sqlserver, 5개 신규 엔드포인트)
`sqlserveradddrsecondary` / `listdrsecondaries` / `promotedrsecondary` /
`syncdrsecondarystate` (+ `sqlservermanualbackup`) — SQL Server에 재해복구
세컨더리 기능이 신설됨. 기존 커버리지 없음(신규 기능이라 당연) — 커버리지
기회로만 기록.

### ⑧ DBaaS 인스턴스 단위 신규 액션 — 9개 엔진 공통 패턴
`{engine}showinstance` (`GET /v1/clusters/{cluster_id}/instances/{instance_name}`)
— cachestore/epas/eventstreams/mariadb/mysql/postgresql/searchengine/sqlserver/
vertica **9개 엔진 전부**에 동시 추가. `{engine}manualbackup`
(`POST /v1/clusters/{cluster_id}/backups/manual`)도 epas/mariadb/mysql/
postgresql/searchengine/sqlserver **6개 엔진**에 동시 추가(기존
`{engine}setbackup`은 백업 "정책" 설정이고 이건 즉시 실행 수동 백업으로
별개). `{engine}upgrademajorversion`
(`PUT /v1/clusters/{cluster_id}/major-version-upgrade`)은 mariadb/mysql
2개(기존 `upgradekernel`과 별개, 메이저 버전 업그레이드). 전부 신규 = 기존
커버리지 없음, breaking 아님 — 커버리지 기회.

### ⑨ KMS ACL 필드 + 신규 엔드포인트 2개
`security/kms/createkey`에 옵션 `acl_cidr`/`private_acl_enabled`/
`private_acl_resources` 추가 + 독립 엔드포인트
`kmssetaclcidr`(`PUT /v1/kms/transit/{key_id}/acl-cidr`) /
`kmssetprivateacl`(`PUT /v1/kms/transit/{key_id}/private-acl`) 신설 —
secretsmanager에 이미 있던 `setsecretaclcidr`/`setprivateacl` 패턴을 KMS에도
이식한 것으로 보임. 전부 옵션/신규라 breaking 아님.

### ⑩ 삭제: `security/secretsmanager/createsecretsmanagerkmskey`
`POST /v1/secrets/kms-key`가 문서 nav에서 완전히 사라짐. **이미 알려진 사실과
정합적** — `knowledge/services.md:1414`에 "CONFIRMED 404 NotFound... Permanent
blocker" (2026-06-23 검증 기록)라고 이미 적혀 있던 엔드포인트다. 라이브에서
원래 라우팅 안 되던 것을 문서에서도 이제 공식 제거한 것으로, **회귀 리스크
없음** — 오히려 문서와 실측이 이제 일치한다. `security__secretsmanager.json`의
`create-kms-key` 스텝은 이미 404를 허용하는 optional 스텝이라 그대로 둬도
무방(제거해도 됨, 판단은 오케스트레이터/담당 에이전트 몫).

### 기타 (breaking 아님, 정보용)
- `container/ske/createcluster`: `security_group_id_list`가 **required→optional**로
  완화(하위호환, 문제없음).
- `container/ske/createnodepool`: 옵션 `preferred_ips`/`subnet_id`/`zone` 추가
  (신규 엔드포인트 `setnodepoolpreferredips`와 세트 — 노드풀 zone/IP 고정
  기능).
- `data-analytics/data-flow/createdataflow`, `data-ops/createdataops`:
  `ingress_controller_name`이 **required→optional**로 완화(하위호환).
- `management/iam-identity-center/createinstance`: 옵션
  `self_managed_password` 추가.
- `networking/direct-connect/createdirectconnect`,
  `networking/vpc/createtransitgatewayfirewall`: 옵션
  `uplink_active_zone`/`uplink_standby_zone` 추가.
- `management/servicewatch`: 신규 엔드포인트 6개(alerts/알림/네임스페이스별
  집계/로그그룹 export) — 옵저버빌리티 기능 확장, 신규라 breaking 아님.
- `compute/virtualserver/updateserverinterface`
  (PUT /v1/servers/{server_id}/interfaces/{port_id}): `fixed_ip_address`가
  optional→**required**로 강화됐지만, **이 엔드포인트 자체가 현재 전체
  코드베이스 어디에도 시나리오/lifecycle 스텝으로 존재하지 않음**(grep 전체
  0건) — 기존 커버리지 갭이라 "깨지는 시나리오"는 없음. 향후 이 엔드포인트에
  대한 write 스텝을 추가할 때 유의해야 할 사항으로만 기록.
- 필드 델타 없이 페이지만 새 버전인 3건(`ai-ml/aimlops-platform/
  releaseaimlopsplatformv1`, `compute/baremetal/createbaremetals`,
  `compute/multinodegpucluster/creategpunodes`) — 내가 쓴 빠른 정규식
  휴리스틱(필드명+required/optional 텍스트 매칭)이 놓쳤을 수 있음(§8).

## 3. 전체 추가/삭제 엔드포인트 목록

### 추가 (45)
```
container/ske/setnodepoolpreferredips                    PUT  /v1/nodepools/{nodepool_id}/preferred-ips
data-analytics/eventstreams/eventstreamsshowinstance      GET  /v1/clusters/{cluster_id}/instances/{instance_name}
data-analytics/searchengine/searchenginemanualbackup      POST /v1/clusters/{cluster_id}/backups/manual
data-analytics/searchengine/searchengineshowinstance      GET  /v1/clusters/{cluster_id}/instances/{instance_name}
data-analytics/vertica/verticashowinstance                GET  /v1/clusters/{cluster_id}/instances/{instance_name}
database/cachestore/cachestoreshowinstance                GET  /v1/clusters/{cluster_id}/instances/{instance_name}
database/epas/epasmanualbackup                            POST /v1/clusters/{cluster_id}/backups/manual
database/epas/epasshowinstance                            GET  /v1/clusters/{cluster_id}/instances/{instance_name}
database/mariadb/mariadbmanualbackup                      POST /v1/clusters/{cluster_id}/backups/manual
database/mariadb/mariadbshowinstance                      GET  /v1/clusters/{cluster_id}/instances/{instance_name}
database/mariadb/mariadbupgrademajorversion               PUT  /v1/clusters/{cluster_id}/major-version-upgrade
database/mysql/mysqlmanualbackup                          POST /v1/clusters/{cluster_id}/backups/manual
database/mysql/mysqlshowinstance                          GET  /v1/clusters/{cluster_id}/instances/{instance_name}
database/mysql/mysqlupgrademajorversion                   PUT  /v1/clusters/{cluster_id}/major-version-upgrade
database/postgresql/postgresqlmanualbackup                POST /v1/clusters/{cluster_id}/backups/manual
database/postgresql/postgresqlshowinstance                GET  /v1/clusters/{cluster_id}/instances/{instance_name}
database/sqlserver/sqlserveradddrsecondary                POST /v1/clusters/{cluster_id}/dr-secondaries
database/sqlserver/sqlserverlistdrsecondaries             GET  /v1/clusters/{cluster_id}/dr-secondaries
database/sqlserver/sqlservermanualbackup                  POST /v1/clusters/{cluster_id}/backups/manual
database/sqlserver/sqlserverpromotedrsecondary            PUT  /v1/clusters/{cluster_id}/dr-secondaries/promote
database/sqlserver/sqlservershowinstance                  GET  /v1/clusters/{cluster_id}/instances/{instance_name}
database/sqlserver/sqlserversyncdrsecondarystate          POST /v1/clusters/{cluster_id}/dr-secondaries/sync-state
management/servicewatch/createloggroupexporttask          POST /v1/log-groups/export-tasks
management/servicewatch/listalertnotifications            GET  /v1/alerts/{id}/notifications
management/servicewatch/listalerts                        GET  /v1/alerts
management/servicewatch/listnamespacebyalerts             GET  /v1/alerts/namespaces
management/servicewatch/setalertnotifications             PUT  /v1/alerts/{id}/notifications
management/servicewatch/showalertlevelcountsbyservice      GET  /v1/alerts/level-counts-by-service
networking/cssdlan/setexternalcssdlannetworkchangeinfo    [UNRESOLVED — 문서 사이트 404, §0]
networking/loadbalancer/setlblistenercertificate          PUT  /v1/lb-listeners/{listener_id}/certificate
networking/loadbalancer/setlblistenerresource             PUT  /v1/lb-listeners/{listener_id}/resource
networking/loadbalancer/setlblistenerrule                 PUT  /v1/lb-listeners/{listener_id}/rule
networking/security-group/addaddressgroupcidrs            PUT  /v1/address-groups/{address_group_id}/add_addresses
networking/security-group/createaddressgroup              POST /v1/address-groups
networking/security-group/deleteaddressgroup              DELETE /v1/address-groups/{address_group_id}
networking/security-group/listaddressgroupcidrs           GET  /v1/address-groups/{address_group_id}/addresses
networking/security-group/listaddressgroups               GET  /v1/address-groups
networking/security-group/removeaddressgroupcidrs         PUT  /v1/address-groups/{address_group_id}/remove_addresses
networking/security-group/setaddressgroup                 PUT  /v1/address-groups/{address_group_id}
networking/security-group/showaddressgroup                GET  /v1/address-groups/{address_group_id}
security/configinspection/getchecklistversion             GET  /v1/configinspection/diagnosis/checklist/version
security/kms/kmssetaclcidr                                PUT  /v1/kms/transit/{key_id}/acl-cidr
security/kms/kmssetprivateacl                              PUT  /v1/kms/transit/{key_id}/private-acl
storage/archivestorage/deletearchivingpolicy               DELETE /v1/archiving-policies/{archiving_policy_id}
storage/filestorage/listvolumereplicationzones             GET  /v1/replications/zones
```

### 삭제 (1)
```
security/secretsmanager/createsecretsmanagerkmskey        POST /v1/secrets/kms-key   (§2 ⑩ — 기존에도 404, 무해)
```

## 4. 시나리오/lifecycle 영향 — 파일별 상세 (수정은 안 함, 식별만)

수정이 필요한 것으로 보이는 20개 파일 (48개 미수정 스텝):

```
regression/scenarios/scenarios.json                                  (natgw-create, publicip 5곳, mysql/postgresql/asg 등)
regression/scenarios/lifecycles/compute__virtualserver.json           (create-server-interface 무관/create-auto-scaling-group)
regression/scenarios/lifecycles/compute__virtualserver-actions.json   (create-server)
regression/scenarios/lifecycles/compute__virtualserver-autoscaling.json (create-auto-scaling-group)
regression/scenarios/lifecycles/data-analytics__searchengine.json     (create-cluster-waiver)
regression/scenarios/lifecycles/data-analytics__vertica.json          (create-cluster-waiver)
regression/scenarios/lifecycles/database__subops-full.json            (epas/cachestore/postgresql create x9)
regression/scenarios/lifecycles/generated__heavy-dbaas.json           (epas replica/restore/upgrade x3)
regression/scenarios/lifecycles/generated__heavy-pg.json              (postgresql replica/restore/upgrade x3)
regression/scenarios/lifecycles/generated__heavy-vs.json              (create-block-volume, fs-create-volume, create-server)
regression/scenarios/lifecycles/generated__light-batch2.json          (create-endpoint-subnet — VPC_ENDPOINT category, §2③)
regression/scenarios/lifecycles/generated__pilot.json                 (create-subnet 무관/create-public-ip)
regression/scenarios/lifecycles/generated__wave2.json                 (create-filestorage-volume)
regression/scenarios/lifecycles/generated__wave4.json                 (create-auto-scaling-group, enabled:false)
regression/scenarios/lifecycles/generated__wave5-net.json             (create-endpoint-subnet — VPC_ENDPOINT category, enabled:false)
regression/scenarios/lifecycles/networking__vpc.json                  (create-subnet x4, create-publicip x2, vpc-endpoint)
regression/scenarios/lifecycles/security__configinspection.json       (save, request)
regression/scenarios/lifecycles/storage__baremetal-blockstorage.json  (create-volume, create-volume-group, create-*-replication)
regression/scenarios/lifecycles/storage__filestorage.json             (create-volume, create-replication)
regression/scenarios/lifecycles/storage__parallel-filestorage.json    (create-volume)
```

**대응하는 `knowledge/formal/resources/*.yaml`** (동일 문제, grep으로만 확인,
수정 안 함):
- `networking__vpc.yaml` — subnet 노드(§2③ 이미 category:PRIMARY 반영됨,
  endpoint-subnet은 미반영), natgateway 노드(§2④ `publicip_id` 구 필드,
  라인 155), publicip 노드(zone 누락)
- `compute__virtualserver.yaml` — server/volume/autoscalinggroup 생성 노드
  (zone/placement_strategy/zones 누락 추정, 미확인)
- `database__postgresql.yaml`, `database__epas.yaml`, `database__cachestore.yaml`,
  `data-analytics__searchengine.yaml`, `data-analytics__vertica.yaml` —
  create-cluster 노드(service_watch_log_collection 누락 추정, 미확인)
- `security__configinspection.yaml`, `storage__baremetal-blockstorage.yaml`,
  `storage__filestorage.yaml`, `storage__parallel-filestorage.yaml` — 동일 패턴

(yaml 쪽은 시간 제약상 각 노드 바디를 전부 열어 확인하지는 못했다 — 위
서비스 목록에 해당 파일이 있다는 것과 vpc.yaml 2건만 직접 grep 확인함. 담당
에이전트가 lifecycle JSON 수정 시 세트로 확인 권장.)

## 5. 리프레시된 데이터 아티팩트

| 파일 | 내용 |
|---|---|
| `data/api_catalog.json` | 1,372 → **1,416개**로 전량 교체 (커밋 대상, 오케스트레이터 검수 대기) |
| `data/.apiref_index.html` | 강제 재수집 캐시(신규 nav 링크 포함) |
| `data/api_bodies.json` | 172 → **192개** (40개 breaking 후보 엔드포인트 중 20개 신규 + 20개 stale 교체) |
| `data/api_docs.json` | endpoints 1,372(내용 40개 갱신) / models **2,306 → 2,346개** |
| `data/api_catalog_params.json` | **리프레시 안 함** — `spec.enrich_catalog` 별도 도구 소관, 스코프 아웃(§8) |
| `data/api_versions.json` | **손대지 않음** (지시대로, 다른 에이전트 소유) |

백업(구 스냅샷, 리포 밖): `/tmp/.../scratchpad/backup-20260715/*.old.json`
(`api_catalog.old.json`, `api_bodies.old.json`, `api_docs.old.json`,
`api_catalog_params.old.json`) — diff의 baseline으로 사용, 커밋 대상 아님.

**커밋 안 함** — 지시대로 워킹트리에만 남겨둠(`git status --short data/`:
`.apiref_index.html`, `api_bodies.json`, `api_catalog.json`, `api_docs.json`
4개 modified).

## 6. 미해결 질문 / 후속 권장 사항

1. **932개 시그니처 변경 중 미검증 892개**: 이번 세션은 write 엔드포인트의
   요청 바디 스키마 변경(40개)만 정밀 검증했다. 892개는 (a) 순수 페이지
   버전 노이즈(§1.3 표본 검증 근거)일 가능성이 높지만, (b) GET 응답 스키마나
   쿼리 파라미터가 조용히 바뀐 케이스가 섞여 있을 가능성을 완전히 배제하지
   못한다. 후속: conformance 축(정적 분석)이나 여유 있는 spec-intel 세션에서
   `spec.scrape_docs --only endpoints`를 write 외 다른 서비스로 확대해
   샘플링 늘리는 것을 권장.
2. **필드 델타 없이 페이지만 새 버전인 3건** (`aimlops-platform/
   releaseaimlopsplatformv1`, `baremetal/createbaremetals`,
   `multinodegpucluster/creategpunodes`) — 내 정규식 휴리스틱이
   `required`/`optional` 텍스트 인접 매칭이라 필드명이 문장 중간에 끼거나
   테이블 렌더링이 다르면 놓칠 수 있다. `spec.scrape_docs`의 정식
   `parse_model()`(TableExtractor 기반)으로 재확인 권장(이번엔 시간상
   40개 중 이 3개는 정식 파서로 재확인 못 함 — 나머지 37개는 §5에서 이미
   정식 파서로 병합함).
3. **KMS `createkey`의 `acl_cidr`/`private_acl_enabled` 실제 검증 형식** —
   secretsmanager 쪽 동일 필드가 "acl_cidr는 /25 이상이어야 함"(2026-06-23
   검증 기록, `knowledge/validated-facts.md`)이었다. KMS도 같은 제약일
   가능성이 높으나 옵션 필드라 당장 breaking은 아니므로 이번엔 미검증.
4. **45개 신규 엔드포인트의 bodies/docs**: 카탈로그 시그니처(method/path)만
   확보했고, 신규 엔드포인트 자체의 상세 문서(요청 바디 스키마 등)는 아직
   `data/api_docs.json`/`api_bodies.json`에 반영 안 함(Address Group 8개,
   SQL Server DR 5개, servicewatch 6개 등). 커버리지를 새로 작성할 담당
   에이전트가 있다면 그 전에 해당 서비스만 좁혀
   `spec.scrape_docs --key-substr <service>` / `spec.extract_bodies
   --key-substr <service>`로 채우는 것을 권장.
5. **`networking/cssdlan/setexternalcssdlannetworkchangeinfo` 404**: 문서
   사이트 자체의 nav-vs-page 불일치로 보인다(3회 재확인 동일). 플랫폼팀 보고
   대상일 수 있으나 우리 쪽 조치 범위 밖 — `spec.summary`의
   "unresolved=0" 가드레일이 이번엔 1로 나온다는 점만 인지 필요(catalog는
   여전히 나머지 1,415개 전부 해석됨, 전체 신뢰도에 영향 없음).

## 7. 권고 조치 (수행은 안 함 — 각 담당에게 hand-off)

- **networking/vpc, storage/*, compute/virtualserver, security/configinspection,
  data-analytics/searchengine·vertica, database/cachestore·epas·postgresql**
  담당 에이전트: §4 목록의 lifecycle JSON에 zone/service_watch_log_collection/
  category(VPC_ENDPOINT)/publicip_ids/check_list_version_id를 추가한 뒤
  라이브 재검증(400→2xx 확인) — CLAUDE.md Hard Rule #7(같은 커밋에
  knowledge/ 갱신)에 따라 진행.
- **database/sqlserver**: 이미 수정됨, 별도 조치 불필요(§2②).
- **security/secretsmanager**: `createsecretsmanagerkmskey` optional 스텝
  제거 여부는 판단 필요(기능적으로는 이미 무해, 정리 차원).
- **coverage 담당**: 45개 신규 엔드포인트(§3) 중 특히 Address Group(8),
  SQL Server DR(5), DBaaS showinstance/manualbackup(15) 순으로 lifecycle
  작성 우선순위 제안 — 전부 신규 기능이라 파급 리스크는 없고 순수 커버리지
  증가분.

## 8. 후속 스크랩 완료 (2026-07-15, 오너 후속 지시 — §6 미해결 (2)(3)(4) 정리)

**신규 45개 엔드포인트 문서 스크랩: 45/45 완료.** `spec.scrape_docs --only
endpoints`(키 필터 없이 실행 — 이미 캐시된 1,371개는 자동 skip되고 정확히
신규 45개만 대상이 됨)로 전부 fetch. `networking/cssdlan/
setexternalcssdlannetworkchangeinfo` 1건은 페이지 자체가 404라 빈
description/parameters로 기록됨(§0/§6④ 그대로, 아래 참고). **요청 바디
모델 스크랩**: 45개 중 write 메서드(POST/PUT/PATCH)는 23개 — `spec.
extract_bodies --key-substr <key>`로 **23/23 전부 예시 바디 확보**
(DELETE 2개는 원래 바디 없음, GET 18개는 해당 없음, cssdlan 1개는 제외).
연관 서비스 18개(container/ske, data-analytics/{eventstreams,searchengine,
vertica}, database/{cachestore,epas,mariadb,mysql,postgresql,sqlserver},
management/servicewatch, networking/{loadbalancer,security-group},
security/{configinspection,kms}, storage/{archivestorage,filestorage})의
참조 모델 페이지도 `--only models --key-substr <service>`로 채워
`data/api_docs.json`의 `models` **2,346 → 2,468개(+122)**. `endpoints`는
**1,372 → 1,417개(+45)**. `data/api_bodies.json`은 **192 → 215개(+23)**.

**미해결 (2) 재검증 결과 — 3개 전부 실제 breaking 델타 있음(휴리스틱이
놓친 것 확인).** 정식 파서(`parse_model`)로 세 엔드포인트의 바디 모델을
다시 열어보니 **최상위 필드 목록 자체는 안 바뀌었지만, 중첩(nested)
서브모델의 `schema_ref`가 새 버전으로 교체돼 있었다** — 이게 정규식
휴리스틱이 "필드 델타 없음"으로 오판한 원인:
- `ai-ml/aimlops-platform/releaseaimlopsplatformv1`: 바디의
  `node_pool_create_request` 필드가 참조하는 서브모델이
  `NodePoolCreateRequest`→`NodePoolCreateRequestV1Dot2`로 교체.
  신구 버전 필드 diff = **`zone`(string, required) 신규 추가만** —
  §2① 존(zone) 웨이브가 여기에도 미치고 있었다(미포함이었던 11번째 케이스).
- `compute/baremetal/createbaremetals`: `server_details` 배열 원소 타입이
  `ServerDetailsRequest`→`ServerDetailsRequestV1Dot2`로 교체. diff = **`zone`
  (required) 추가 + `use_hyper_threading` 필드 완전 삭제**(대체 필드 없음,
  단순화로 추정).
- `compute/multinodegpucluster/creategpunodes`: `server_details` 배열 원소
  타입이 `GpuNodeDetailsRequest`→`GpuNodeDetailsRequestV1Dot3`로 교체.
  diff = **`zone`(required) 추가만**.

세 신규 서브모델 페이지(`nodepoolcreaterequestv1dot2`,
`serverdetailsrequestv1dot2`, `gpunodedetailsrequestv1dot3`)는 **메인
nav 트리에 링크가 없어** 기존 `discover_models()`(nav 파싱 기반)가 원래
못 찾던 페이지였다(라이브에는 실재함, `<h1>`으로 확인) — 개별 fetch 후
`data/api_docs.json.models`에 수동 병합(2,468→**2,471**). 이 3개
엔드포인트는 §2①의 zone 필수화 파급 범위에 **추가로 포함**돼야 한다
(기존 §2① 10개 + 이 3개 = 13개, `ai-ml/aimlops-platform`·
`compute/baremetal`·`compute/multinodegpucluster` 3개 서비스가 재테스트
대상에 새로 추가됨 — 다만 이 3개 서비스 모두 현재 lifecycle에 create
스텝이 없다면 즉시 breaking 위험은 없음, coverage 담당 확인 필요).

**미해결 (3) cssdlan 404 재시도 결과: 그대로 404, 보고만.** 1회 재확인
(`Range` 요청 + 풀 GET 요청 둘 다) — `<title>404 Page not found | Samsung
Cloud Platform Documentation</title>` 동일, 3회 누적(원 세션 3회 + 이번
1회 = 총 4회) 전부 동일 결과. 우리 쪽 fetch 문제 아님, 문서 사이트 자체의
nav-vs-page 불일치 — 조치 범위 밖, 그대로 유지.

**검증**:
- `python -m spec.summary` → `unresolved: 1`(가드레일 ≤1 유지, cssdlan
  1건 그대로).
- `data/api_docs.json`, `data/api_bodies.json` JSON 파싱 정상.
- `python -m spec.coverage_gap` → 실행 중 `AttributeError:
  'NoneType' object has no attribute 'split'`로 **크래시 발견**(cssdlan의
  `http_path: null`을 `norm_path()`가 처리 못 함 — 이번에 카탈로그에
  처음 생긴 unresolved 엔트리라 지금까지 노출 안 됐던 기존 버그). `spec/
  coverage_gap.py`의 `load_catalog()`에 `http_path`가 없는 엔트리를
  스킵하는 가드를 추가해 수정(코드 변경, `data/api_catalog.json`은 안
  건드림). 같은 클래스 버그가 `spec/extract_bodies.py`에도 있어
  (`e["method"].upper()`가 `method: null`에서 크래시) 함께 가드 추가.
  **수치는 이 세션 중에도 두 번 달랐다** — 다른 에이전트가 동시에
  `regression/scenarios/lifecycles/generated__newapi-addressgroup.json`
  `generated__newapi-servicewatch.json` 등을 병행 작성 중이라(git status로
  확인, 우리 소유 파일 아님, 손 안 댐) 첫 실행(전체 갭 774개, 신규 45개 중
  38개가 갭)과 이 문단 작성 시점 재실행(아래 최종치) 사이 값이 달라짐 —
  **아래는 최종 재실행 기준**: 전체 갭 **38개**(엔드포인트 1,415개 중
  1,377개=97.3% reachable). 신규 45개 중 cssdlan 제외 44개를 대조하면
  **29개가 여전히 커버리지 갭**(DBaaS showinstance/manualbackup/
  upgrademajorversion 9엔진 계열 다수, SQL Server DR 6개, KMS ACL 2개,
  LB listener 3개, `container/ske/setnodepoolpreferredips`,
  `storage/archivestorage/deletearchivingpolicy` 등), **15개는 이미
  reachable** — 그중 9개(servicewatch alerts 5개, configinspection
  checklist-version, filestorage zones)는 path param 없는 GET이라 스모크
  플로어가 자동 커버, 나머지 6개(Address Group `addaddressgroupcidrs/
  createaddressgroup/deleteaddressgroup/listaddressgroupcidrs/
  removeaddressgroupcidrs/setaddressgroup/showaddressgroup` 등)는 다른
  에이전트가 이미 `generated__newapi-addressgroup.json` lifecycle을
  작성해 병합한 결과로 보임(실시간 관측, 우리 작업 아님 — coverage 담당이
  라이브 재검증 여부 확인 권장).

**아티팩트 변경**: `data/api_docs.json`(endpoints 1,372→1,417, models
2,346→2,471), `data/api_bodies.json`(192→215), `data/.apiref_index.html`
(변경 없음, 이번 세션은 nav 재수집 안 함 — 이미 최신). `spec/
extract_bodies.py`, `spec/coverage_gap.py`에 None 가드 추가(버그 수정,
코드). **커밋 안 함** — 오너 지시(git commit 금지).
