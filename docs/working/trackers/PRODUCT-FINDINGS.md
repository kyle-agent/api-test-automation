---
status: active
for: validation
---

# PRODUCT-FINDINGS — consolidated ledger of product/API findings

- Date: 2026-06-12 · Status: **active** (append-only — new findings get the next id)
- Scope: things the R3 verification waves found that are **about the product,
  not about our tests** — backend bugs, spec/docs defects, IAM-policy gaps,
  service quirks, and live confirmations of userguide facts. One row per
  finding. The per-node detail (exact bodies, retry semantics) stays in the
  node `notes` of `knowledge/formal/resources/*.yaml` and in
  `data/baselines/known_issues.json`; this file is the cross-service index.
- Relationship to other docs: wave-level narrative in
  `docs/RESOURCE-MODEL-PLAN.md` §6; baselined entries (muted on the dashboard)
  in `data/baselines/known_issues.json`; AXIS-2 conformance findings
  (`reports/results/findings.jsonl`) are a separate, automated stream — this
  ledger is the curated, human-triaged one.

## Ledger

| id | service / endpoint | symptom | evidence (run id) | class | status |
|----|--------------------|---------|-------------------|-------|--------|
| PF-01 | management/servicewatch · `GET /v1/log-groups/{id}/log-streams` (LIST) | 403 `Action definition is not found` — the LIST is unauthorizable while the per-id GET works | 27394211896 | missing-IAM-action-definition | open — verify rerouted to per-id GET (node note) |
| PF-02 | application-service/apigateway · `GET …/api-keys/{key_id}` (per-id) | 403 `Action definition is not found` — per-id GET unauthorizable while LIST works | 27394211896 | missing-IAM-action-definition | open — verify keeps `[200,403]` to track it |
| PF-03 | storage/filestorage · `GET /v1/snapshots/{id}` (per-id) | 403 `Action definition is not found` — third case of the same class | 27399448835 | missing-IAM-action-definition | open (node note) |
| PF-04 | financial-management/budget · `POST /v1/budgets/account` | 500 `ContactAdminForAssistance` on a complete-looking body — a validation problem must not 500 | 27395331657 | product-bug (5xx on valid-shaped input) | **baselined** Product Bug in `known_issues.json`; node out of composed waves |
| PF-05 | devops-tools/devopsservice · `POST /v1/devops-services` | 400 ValidationError with `["Field required"]`×3 **without field names** — the error schema hides which fields are missing | 27394211896 | error-schema-defect (also an AI-usability case) | open — node disabled pending userguide research |
| PF-06 | compute/scf · `GET /v1/cloud-functions/{id}/logs` · `/metrics` | `must select either time or period` — docs mark **all** params optional, but one of time/period is required (format undocumented) | 27399448835 | undiscoverable-params | worked around (`?time=1h`); docs fix needed |
| PF-07 | management/cloudmonitoring · `GET /v1/cloudmonitorings/accounts/products…` | 400 `InvalidHeaderValue 'X-ResourceType'` — a **required header that appears nowhere in the docs** | 27399448835 | undocumented-required-header (new defect class) | worked around — model carries `headers: {X-ResourceType: INSTANCE}`; header accepted in rev 2 |
| PF-08 | container/scr · `PUT /v1/container-registries/{id}/private-acl` | 500 `ContactAdminForAssistance` on a doc-valid body — reproduced 3× | 27401527554 · 27417986669 · 27421363609 | product-defect candidate (5xx, possibly an Editing-state race) | open — verify removed from the composed chain; retry-style verify is an R3 follow-up |
| PF-09 | security/kms + security/secretsmanager · `DELETE` key/secret | Deletion is **scheduled**, not immediate: deleted (2xx) items stay in lists for their pending-deletion window (gone from console) | 27401527554 (sweep log, 5 rounds) | service-quirk (scheduled deletion) | recorded in service quirks; sweep no longer counts re-deletes as progress |
| PF-10 | container/scr · `POST /v1/container-registries` | 403 quota `CONTAINER_REGISTRY.NON_VISIBILITY.MAX` **1EA** — live confirmation of the userguide's max-2-per-account (1 per visibility type) rule | 27421363609 (docker probe) | userguide-fact-confirmed (not a defect) | closed — probe now borrows an existing Running registry on quota-403 |
| PF-11 | compute/scf · `DELETE /v1/triggers/{id}` body `trigger_type` | API requires `'cron'`; the hand-written probe sent `'cronjob'` and had been **silently 400ing for weeks** behind a tolerant `expect [.., 400]` | 27417986669 (unmasked) | masked-defect (harness) | fixed — model sends `cron`; lesson below |
| PF-12 | compute/virtualserver · `POST …/interfaces/{port_id}/static-nats` body | Real field is `publicip_id`; the probe's `public_ip_id` had been silently 400ing behind a tolerant expect — the live ValidationError handed us the field name | 27421363609 | masked-defect (harness) | fixed — model sends `publicip_id` (heavy rev 3 dispatched) |
| PF-13 | compute/virtualserver · `POST …/interfaces/{port_id}/static-nats` | 400 `VirtualServer.InvalidVpcPublicIp` "The VPC should have at least one **Internet Gateway** when attaching Internet NAT" — precondition not in the userguide/API docs for this endpoint | 27424991237 | undiscoverable-params (undocumented prerequisite) | fixed — `internet-gateway` added to `server-static-nat` requires; heavy rev 4 |
| PF-14 | container/scr · `GET /v1/repositories/check-duplication/name` | `name` alone → 400 `ValidationError ["Field required"]` without naming the field; repo names are registry-scoped so `registry_id` is required — neither the requirement nor the field name is discoverable from the error or docs | 27424991237 | error-schema-defect + undiscoverable-params | fixed — model sends `registry_id` query param |
| PF-15 | compute/virtualserver · `GET …/static-nats/{nat_id}` | 403 "Action definition is not found" on the per-id read right after a successful create — 4th instance of the missing-IAM-action-definition class (with servicewatch log-streams LIST, iam api-key per-id GET, fs snapshot read) | 27450089575 | missing-IAM-action-definition | worked around — verify expects [200,403] with this note |

| PF-16 | container/scr · docker `login` to the registry endpoint | DNS resolved + registry reachable, but SCP access/secret keys → `unauthorized: authentication required`. **Settled**: SCP platform keys are NOT the docker credential — a separate console-issued SCR auth key is required (owner hypothesis 2026-06-12 disproven) | 27452095757 | entitlement (console-issued credential) | closed — cloud-ml chain stays gated on scr-auth-key; probe retired |
| PF-17 | compute/virtualserver · `POST /v1/servers/{id}/password` | 400 `VirtualServer.InvalidPrivateKeyValue` 'Failed to decrypt' with the correct, server-bound keypair private_key. **CORRECTED 2026-07-08 (owner challenged the Windows-only claim)**: the API docs state NO OS precondition, and the 07-08 E2E servers were in fact **Windows 2016** (`images[0]`=00799e76, `keypair_name` bound) — the earlier "Linux has no admin password (Windows-only)" explanation is REFUTED for those runs. Both 400s fired 2.8s / 20.6s after ACTIVE (req-714ca840 / req-80845d28) → likely cause: the guest publishes the encrypted password minutes after first boot; querying before publish fails. Conformance angles: the publish-latency precondition is UNDOCUMENTED, and `InvalidPrivateKeyValue` misdescribes "not yet available". Side-finding: recipes pick `$.images[0]` blindly and the list currently leads with Windows images — OS is order-dependent (explains the June/July confusion). | 27452095757 · E2E 20260708-010208 (req-714ca840 · req-80845d28) | undocumented-precondition + error-schema-defect (misleading code); was: service-quirk (OS-conditional) | **superseded same day by owner decision**: fleet pinned to ubuntu (find-image `os_distro=ubuntu` — fast boot, no Windows-license rate), password call stays single-shot with the real key; on ubuntu the guest never publishes an encrypted admin password so 400 is the expected terminal state — the until-200 poll was removed as wall-time waste. 2xx now structurally unreachable on this fleet → reachability-waiver candidate. **Owner 2026-07-08: Windows 1-VM 실험은 나중에 (보류)** — waiver 미제출 유지, 갭 칩 상태가 의도된 표시 |
| PF-18 | management/resourcemanager · `GET /v1/resources/{srn}` (show-resource) | 403 `Forbidden / You do not have permission to Action` — 5th instance of the missing-IAM-action-definition class (with servicewatch log-streams LIST PF-01, fs snapshot read PF-03, VS static-NAT per-id read PF-15) | 27466988779 | missing-IAM-action-definition | worked around — verify expects [200,403] with this note |
| PF-19 | application-service/apigateway · `PUT /v1/apis/{api_id}/resource-policies` (setresourcepolicy) | 500 `ContactAdminForAssistance` with the proven policy_document shape — a write previously thought VERIFIED now 5xxes | 27466988779 | product-bug (5xx on valid-shaped input) | **baselined** Product Bug in `known_issues.json`; composed step tolerates 500. **2026-07-09 재해석(오너 가이드)**: 보낸 Resource SRN이 형식 불량(`srn:scp::…` 4-세그먼트) — 정식 SRN으로 교정 후 재검증 대기; 여전히 남는 결함은 '불량 입력에 400 아닌 500' |
| PF-20 | management/iam · `POST /v1/roles` (createrole) | 500 `ContactAdminForAssistance` even with a self-created policy bound — the earlier policy_ids: [] hypothesis was wrong; it 5xxes with a bound policy too | 27466988779 | product-bug (5xx on valid-shaped input) | chain `gen-wave5-iam-role` disabled (enabled:false + _disabled_reason); iam-role node kept modeled |
| PF-21 | compute/virtualserver · `POST /v1/volume-transfer` (create-volume-transfer, gen-wave2-volume) | 500 `InternalServerError` on a complete-looking body (name + real volume_id) — a NEW 5xx-on-create; req-bc27a3b6 | 27540589368 (req-bc27a3b6) + 27583285457 (req-78aec306) | **CONFIRMED 2026-06-15 (2nd repro run 27583285457); baselined in known_issues.json** | baselined Product Bug `compute/virtualserver/createvolumetransfer` in `known_issues.json`; the virtualserver model `volume-transfer` create tolerates 500 (`optional:true, expect_status:[200,202,500]`) so the chain survives. |
| PF-22 | networking/dns · `POST /v1/public-domains` (create-public-domain, networking-dns-public-domain) | 500 `InternalServerError` on create — a NEW 5xx-on-create (1 sighting); req-cfff60bb-5e1d-4b66-94d4-c7f7eef548ff. Step is already optional/group-skipped so it is NOT a test failure, just logged. | 27593608514 (req-cfff60bb-5e1d-4b66-94d4-c7f7eef548ff) | product-bug candidate (5xx on create) | **UNCONFIRMED — pending 2nd repro; not baselined** (NOT in known_issues.json yet; no tolerance edit needed — already optional/group-skipped) |
| PF-23 | application-service/apigateway · `POST /v1/privatelink-endpoints` (createprivatelinkendpoint) | 500 `ContactAdminForAssistance` on a **docs-correct** body. Live docs verification (regression-agent 2026-06-18, req-f088cd1d / req-44f443c7): `privatelinkendpointcreaterequest` requires exactly `{name (^[a-zA-Z0-9]*$, 3..20), privatelink_service_id, description?}` and the scenario sends precisely that — NOT a body-shape gap. A bad/non-existent `privatelink_service_id` must return 400/404, not 500. Same ContactAdmin-class as budget createaccountbudget / apigateway setresourcepolicy (PF-19). | direct probe 2026-06-18 (req-f088cd1d-35bd-4e2c-a5d6-43b7e60162b6) | product-bug (5xx on valid-shaped input) | **baselined** Product Bug `application-service/apigateway/createprivatelinkendpoint` in `known_issues.json`; the `xcov-pl-create` group tolerates 500 (`expect_status` now includes 500) so the lifecycle records the CALL and survives |
| PF-24 | networking/vpc · `POST /v1/ports` (createport) | The **documented default `""`** for `fixed_ip_address` is rejected live: 400 `scp-network.port.fixed_ip.format-error` ("Fixed IP() format is not valid" — the empty parens ARE the empty value), and the docs' own request_example itself sends `""`, so the example is unrunnable exactly as documented. The tolerant expect masked it for weeks while the 400 cascaded: `{std_port_id}` never captured → 6 downstream port endpoints (show/update/delete port, attach/read/detach interface) ran as literal-placeholder 404s — the vs-port domino. | 28723287734 (HB3b) · console2 E2E 20260708-010208 (req-06ef18a7) | undiscoverable-params (doc default/example unrunnable) + masked-defect | worked around — recipe sends an explicit in-CIDR IP `10.135.3.10` in the dedicated port subnet (commit b343694d); whether OMITTING the field also works is still unverified |
| PF-25 | platform/sts · every endpoint (e.g. `POST /v1/assume-role`) | The catalog-advertised **regional** host (`sts.kr-west1...`) does not serve the API at all: it answers API paths with the console's **text/html 404 portal page** — no JSON error envelope, no API semantics. sts is in fact GLOBAL-only; advertising a per-region hostname that falls through to a web portal breaks every client that trusts the catalog's region template. | console2 E2E 20260708-010208 (all sts calls routed regionally) | error-schema-defect (HTML on an API path · regional host advertised for a global-only service) | worked around — client now routes sts to the global host (`core/config.py` DEFAULT_GLOBAL_SERVICES += sts, commit d1f41818) |
| PF-26 | management/cloudmonitoring · `GET /v1/cloudmonitorings/product/v2/accounts/products` | 404 exactly as documented — the documented path carries a **double version segment** (`/v1/...` service prefix + `/v2/` mid-path). The endpoint is unreachable as the catalog/docs spell it, and no single-version variant is documented anywhere. | runs 2026-06-27 · 2026-07-08 (soft-404 both sightings) | service-quirk (catalog/doc path defect — double version segment) | open — recipe keeps the read tolerant; needs a vendor doc fix or discovery of the real path |
| PF-27 | compute/virtualserver · `POST /v1/servers/{id}/images` (createvirtualserverimage) | 400 `VirtualServer.BadRequest` "The request could not be satisfied." — **zero actionable detail** (no field, no reason, no docs pointer; AI-unusable error). Repro ×2 on 2026-07-08, both times the source server ran a **Windows 2016 standard image** (blind `$.images[0]` pick); the store's past 2xx evidence is from Linux-source runs. Hypothesis: imaging a Windows-licensed guest is prohibited but neither documented nor named in the error. Not a name/quota collision (custom-image list empty at repro time). | E2E 20260708-010208 (req-bab670a7) · owner run 20260708-140335 · **ubuntu repro req-59b8e9fe (run 936s)** | error-schema-defect (opaque 400) + undocumented-precondition | **hypothesis CORRECTED same day**: ubuntu source 400'd identically → NOT OS/license. Same run: `-full` imaged its server 202 (no extra volume attached at image time) while netops failed (volume attached first), and `-full`'s image-update returned `Image.InvalidVolumeOnVisibilityUpdate` ("Image **with volumes** cannot update visibility") — leading hypothesis: **a server with attached data volumes cannot be imaged**, undocumented and unnamed in the 400. Recipe reordered (image group before volume attach) — next-run acceptance; opaque-error finding stands |
| PF-28 | data-analytics/quick-query · `POST /v1/quick-query/validate-resources` (validatequickqueryresources) | 500 `ContactAdminForAssistance` on the documented dry-run body — **재현** (2026-06-18 커버리지 런 2×와 동일 서명, known_issues 기존 baselined). 재현경로: gen-quick-query-validate 계열 스텝의 표준 body 그대로. AI-사용성: '자원 생성 가능 여부 검증'이 목적인 **preflight 엔드포인트가 500이면 에이전트는 body 유효성에 대해 아무 신호도 얻지 못한다** — dry-run의 존재 의의 상실. | 2026-07-08 full live run (재현; 최초 2026-06-18 ×2, known_issues.json) | product-bug (5xx on valid-shaped input) | **baselined** (`known_issues.json` 기존 항목 재확인) |
| PF-29 | database/mysql + database/postgresql · `DELETE /v1/clusters/{id}/backups` (unsetbackup) | 401 `AuthNFailed` — **같은 인증 세션에서 다른 모든 sub-op가 2xx인 가운데 이 op만 401**, mysql·postgresql 두 엔진 동시 재현 (기존 run 27346642059와 동일 서명). 재현경로: DBaaS 클러스터 라이프사이클의 backup 설정 후 해제 스텝. 자격증명 문제가 아니라 **이 라우트만 인증 경로가 다른/깨진 결함 의심**. AI-사용성: 에이전트가 401을 신뢰하면 불필요한 재인증 루프에 빠진다 — 오분류된 에러 코드는 자동화에 최악. | 2026-07-08 full live run (mysql·postgresql 동시 재현; 최초 27346642059) | product-bug (auth-path; misclassified 401) | **baselined** (`known_issues.json` mysql/postgresql 항목 재확인) |
| PF-30 | database/cachestore + database/mariadb · `PUT /v1/clusters/{cluster_id}/parameters` (setparametervalues) | 500 `ContactAdminForAssistance` — cachestore·mariadb **두 엔진 동시 재현** = 엔진 공통 parameter 경로의 백엔드 결함 의심 (ContactAdmin 클래스: budget PF-04 · createrole PF-20 가족). 재현경로: 클러스터 정상 기동 후 파라미터 값 수정 스텝. AI-사용성: 파라미터 튜닝은 대표적 자동화 대상인데 5xx는 body가 맞는지조차 알려주지 않는다. | 2026-07-08 full live run (cachestore·mariadb 동시) | product-bug candidate (5xx on valid-shaped input) | open — pending 2nd repro (not yet baselined) |
| PF-31 | management/iam · `POST /v1/roles` (createrole) | 500 `ContactAdminForAssistance` — **PF-20 재재현** (완전한 body, 정책 바인딩 유무 무관). 재현경로: iam-role-full create-role 표준 body. AI-사용성: 역할 생성은 권한 자동화의 관문인데 5xx가 지속되는 한 이 계정에서 role 기반 시나리오 전체가 프로그램적으로 불가능. | 2026-07-08 full live run (재현; 최초 27466988779 = PF-20) | product-bug (5xx on valid-shaped input) | **baselined** (PF-20/`known_issues.json` 재확인; chain은 optional로 격리 유지) |
| PF-32 | management/cloudmonitoring · `GET /product/v2/accounts/products` (getaccountproductlist의 무접두 변형) | 400 `InvalidHeaderValue` — **PF-26 이중버전 가족의 신규 관측**: 문서 경로(`/v1/cloudmonitorings/product/v2/accounts/products`)는 404(PF-26)인데, 접두 없는 `/product/v2/...` 변형은 **라우팅은 되고** 미문서 헤더 요구(400)로 막힌다(PF-07 `X-ResourceType` 클래스). 재현경로: cloudmonitoring 계열 read 프로브. AI-사용성: 문서 경로 404 + 실경로 미문서 + 필수 헤더 미문서의 3중 장벽 — 에이전트가 스스로 도달할 방법이 없다. | 2026-07-08 full live run (PF-26: 2026-06-27·07-08 404 관측과 대조) | service-quirk (catalog/doc path defect) + undocumented-required-header | open — 실경로·헤더 조합 발굴 필요 (PF-26과 병합 추적) |
| PF-33 | ai-ml/aimlops-platform · `POST /v1/aimlops-platform` (releaseaimlopsplatformv1) | 400 — docs 모델(`AiMLOpsPlatformCreateRequest`)은 `initial_user_email`을 **optional**(`any of [string, null]`)로 표기하지만 라이브는 **필수**로 거부. run-2에서 정형 wrapper body가 이 필드 부재만으로 400. AI-사용성: 스키마를 신뢰한 에이전트는 원인 필드를 추정할 수 없다. | run-2 (owner 콘솔 rec 73243c6e, 2026-07-09) | undiscoverable-params (doc-optional-but-required) | worked around — lifecycle body에 필드 추가 (2026-07-09 오프라인, 라이브 확인은 다음 명시-선택 런) |
| PF-34 | networking/vpc · `POST /v1/vpc-endpoints` (createvpcendpoint) — FS 대상 | `resource_key`의 docs 예시가 32-hex id 꼴이라 **볼륨 uuid로 오도**하지만, FS(FileStorage) 타입의 실값은 **파일스토리지 endpoint IP**다 (canonical 소스 = `GET /v1/vpc-endpoints/connectable-resources`, 라이브 실측 반증). uuid를 넣으면 400. | run-2 수리 라이브 실측 (2026-07-09, 커밋 65ecdef2) | undiscoverable-params (misleading doc example) | worked around — lifecycle이 connectable-resources GET에서 실값 캡처; 모델 yaml 반증 주석 동반 |

| PF-35 | networking/loadbalancer · `POST /v1/lb-server-groups/{id}/members` (addlbservergroupmembers) | 400 `InvalidRequestMemberWeight` — docs는 `member_weight` optional·1~1000·예시 100으로 무조건 허용처럼 기술하지만, 서버그룹 `lb_method=ROUND_ROBIN`(비가중)이면 **docs-유효값(100)도 거부**. 에러 제목도 값이 아니라 '조건'이 문제임을 숨긴다 (undocumented conditional constraint + misleading error). 재현: run 2026-07-09 (VM object_id 수리 직후 첫 도달). | owner 콘솔 run (req-f5db2a85-50ad) | undiscoverable-params (조건부 제약 미문서) | **해결 경로 확정 (2026-07-10 라이브)**: weight는 사실상 필수이며(부재도 'None' invalid) **가중 알고리즘 그룹에서만 유효** — WEIGHTED_ROUND_ROBIN + weight 10으로 create 2xx (member PUT 409 settle은 별건, 사다리 처리). 결함 본질 = docs가 optional·무조건 허용으로 기술 + 비가중 그룹 거부·부재 거부를 값 탓으로 오도하는 에러 |
| PF-36 | management/iam · `POST /v1/accounts/{account_id}/users` (createiamuser) · `PUT .../users/{user_id}/password` | 400 `ContactAdminForAssistance` "Password must be encoded base64" — **password는 base64 인코딩 필수인데 문서(iamusercreaterequest)·userguide 어디에도 없음**. 에러 코드도 ContactAdminForAssistance(관리자 문의)라 입력 수리 가능함을 숨긴다. base64 재시도 → 201 즉시 성공 (regrscr 유저, 2026-07-10). | 라이브 실측 (req-5e1ba1d4-3892) | undocumented-required-header (base64 인코딩 규약 미문서) + error-schema-defect (오도성 코드) | worked around — 엔진 json_b64_fields (토큰 치환 후 인코딩), iam-user-full 반영 |
| PF-37 | container/scr · public 레지스트리 토큰 인증 (`auth.scr.public.kr-west1...:/auth/token`) | **문서(CLI 가이드 '인증키로 로그인하기': Username=AccessKey, Password=SecretKey)대로의 유효 인증키를 일관되게 401 invalid credentials로 거부**. 소거 완료: 같은 키로 HMAC Open API 200 (키 유효) · `scr:LoginContainerRegistry` 보유 (ScrPullPushOnlyAccess 기본 포함, 콘솔 JSON 실측) · 유저/키 ACTIVE · owner 본인 구키도 로컬 docker login 실패 · endpoint 리셋 후에도 동일. `account=<user_name>` 파라미터 시 'user and account are not the same(user:<AK>)' — Basic 파싱은 정상임을 증명. | 라이브 실측 2026-07-10 (req-6ed621c7 등 다수) | product-bug (public 토큰 서버가 IAM 인증키 검증 실패) | open — SDS 문의 권고 |
| PF-38 | container/scr · `PUT /v1/container-registries/{id}/enable-public-endpoint` | disable(200)→enable(200), 상태 Editing→Running 정상 전이했으나 **public 엔드포인트가 2시간+ TCP reset으로 미복구** (API는 Running/enabled 표시 — 컨트롤플레인·데이터플레인 불일치). 같은 시간 auth 서버는 정상 응답. | 라이브 실측 2026-07-10 17:11~19:3x KST (registry nayvugfp4154447ab0ab61279cba3d72) | product-bug (endpoint 재활성화 데이터플레인 미반영 + 상태 오표시) | open — 콘솔 재토글/SDS 문의 |
| PF-48 | database(5엔진)+searchengine · `DELETE /v1/clusters/{id}/backups`(unsetbackup) · `PUT /v1/clusters/{id}/backup-histories`(removebackuphistories) | **전 상품 동일 401 `Dbaas.Unauthorized.AuthNFailed`(유효 HMAC)** — 원인 확정(2026-07-18, 오너 콘솔 실측): 백업 파일 삭제는 콘솔에서 **SMS 본인인증(휴대전화 인증번호) 필수**인 인증 승격 작업인데, Open API에는 이 인증 컨텍스트를 전달할 수단이 전무. 문서에는 401만 등재·사유/대체수단 미기술. setbackup POST(동일 경로·서명)는 5엔진 2xx 다수(21~40회)로 클라이언트 소거 완전. 콘솔의 "백업 해제"는 DELETE가 아닌 수정 모달 [사용] 해제(set 플레인) — DELETE는 콘솔 등가 경로도 없음. | 오너 콘솔 스크린샷(본인인증 모달)+verified store 대조 2026-07-18 | **auth-elevation-not-exposed** (인증 승격 절차 API 미노출·미문서) + AI-usability (에이전트가 백업 수명주기를 API로 완결 불가) | closed-as-designed 판정 대기 — waiver(unsatisfiable-flow) 오너 승인 제안 중 |
| PF-49 | security/security-group · `POST /v1/address-groups` (NEWAPI, 2026-07 버전업 신설) | 정형 body(name/description/addresses[CIDR]/tags — 문서 모델 그대로)에 **500 `ContactAdminForAssistance`**. 같은 런에서 동종 500이 kms/transit·quick-query·PLE approval에도 관측 — 신설 API의 서버측 미개통/결함 의심 클래스. | run 20260729-081847-bd2a 실측 (req-7c66b404 / req-e59a77f9) | product-bug 후보 (신설 endpoint 5xx) | open — 다음 런 재확인 후 지속 시 known_issues 등재 + SDS 문의 |
| PF-50 | security/kms · `POST /v1/kms/transit` (NEWAPI, 2026-07 버전업 신설) | 정형 body(key_type=advanced/purpose=rsa-2048/auto_rotate/rotate_cycle)에 **500 `ContactAdminForAssistance`**. **교차-오퍼링 소거 완료(2026-07-29)**: 업그레이드 전 버전의 **타 오퍼링에서도 동일 바디·동일 500** (run 11f2, req-21667c53-c891) — 반면 같은 런에서 `GET /v1/kms/transit/duplicate`는 **200** → transit API 표면은 양쪽 다 배포돼 있고 **create 백엔드만 전역 결함**(계정/오퍼링/롤아웃 문제 아님). 배경: 구 스펙 시절 `GET /v1/managed-kms/transit`는 count:0 + create API 부재로 블로커 확정(2026-07-13). | run bd2a (req-82d6c661) + 타 오퍼링 run 20260729-232115-11f2 (req-21667c53) — 2개 환경 일치 | **product-bug 확정급** (create 플레인 전역 5xx, 조회 플레인 정상) | open — **SDS 문의 권고 상향** (2-env 소거 완료); known_issues 등재 후보 |
| PF-52 | networking/loadbalancer · `POST /v1/lb-health-checks` | **subnet이 non-ACTIVE인 창**(선행 LB create/PUT·subnet-VIP 생성이 subnet을 재전이시킨 동안)에 health-check를 만들면 서버가 **수락 후 영구 Creating 행** — 거부(4xx)도 수렴도 실패전이도 안 하는 상태머신 트랩. 관찰자는 지울 수도 기다릴 수도 없는 좀비 리소스가 남는다. AI-사용성: 수락된 create가 영원히 안 끝나면 에이전트는 타임아웃 소진 외 판단 수단이 없음. | 오너 실측 2026-07-30 (타 오퍼링 west1, 제품 콘솔 관찰) | product-bug (accept-then-hang; 전제조건 미검증) + AI-usability | open — 시나리오 회피 게이트 반영(hc-create 직전 subnet ACTIVE 재확인, heavy-shared-networking·gen-heavy-lb-members). 다음 런이 게이트 유효성 판정; SDS 문의 후보 |
| PF-51 | database mysql/mariadb/postgresql/epas(+α) · `PUT /v1/clusters/{id}/kernel-upgrade` ×5엔진 · `PUT .../major-version-upgrade` ×2 | **405 `Dbaas.MethodNotAllowed`** — 2026-07-02 P3 배치에서 PUT 2xx 실증이 있던 op가 405로 회귀. 2026-07-15 리프레시 문서/카탈로그는 여전히 PUT. 서버측 메서드 변경(또는 기능 제거)이 문서 미반영된 드리프트로 판단. | run bd2a 실측 ×7키 (전 엔진 일관) | docs-drift (메서드 변경 미문서) or feature-removal | open — 다음 런 재확인; 콘솔에서 동일 기능의 HTTP 메서드 실측(개발자도구) 가능하면 확정 |

## The masked-defect lesson (PF-11 · PF-12)

Tolerant expectations (`expect_status: [200, 400]` etc.) on hand-written
"coverage probes" can hide that a step has **never once succeeded** — the row
stays green while the call 400s forever. Two such cases surfaced only when the
composed (model-driven) chains demanded a real 2xx:

- scf trigger delete `trigger_type: cronjob` → the API wants `cron` (PF-11);
- VS static-NAT `public_ip_id` → the API wants `publicip_id` (PF-12).

Rules of thumb adopted: ① composed verify/teardown steps expect success —
widen an expect list only WITH a finding note explaining why (the PF-02
pattern); ② when a live error names a field/enum, encode it in the model
immediately with the run id; ③ treat any long-green tolerant-expect step as
unverified until a 2xx is on record.

## Conventions

- **id** — `PF-NN`, append-only, never reused.
- **class** — missing-IAM-action-definition · product-bug ·
  error-schema-defect · undiscoverable-params · undocumented-required-header ·
  service-quirk · userguide-fact-confirmed · masked-defect.
- **status** — open · worked around · baselined · fixed · closed.
- A finding that gets muted on the dashboard must ALSO be a row in
  `data/baselines/known_issues.json` (PF-04 is the model case).
