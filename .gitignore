__pycache__/
*.pyc
.env
# compose secrets (docs/DEPLOY.md) — only .env.platform.example is committed
.env.platform
# dashboard build artifacts (generated, not source)
dashboard/index.html
dashboard/history.jsonl
dashboard/services/
.claude/*
# but DO track vendored Claude Code skills (see .claude/skills/README.md)
!.claude/skills/
# and the project subagent definitions (e.g. log-optimizer)
!.claude/agents/
# and the shared SessionStart bootstrap hook + project settings (web sessions)
!.claude/hooks/
!.claude/settings.json
# (personal overrides stay local/ignored)
.claude/settings.local.json

# control plane runtime state (controlplane/README.md)
controlplane/data/

# local virtualenv (controlplane/README.md 로컬 실행)
.venv/
# extractor outputs (knowledge/formal/resources/extract.py) — drafts, never curated
knowledge/formal/resources/_drafts/

# generated per-run output (dashboards, static export, results)
reports/
# generated build artifact (sha/time changes every build) — CI/preview regenerate it
poc/scenario-viz/data/context.js
/live.html
/catalog-topology-full.html
/catalog-select-to-plan.html
/dag-run.html
data/optimizer/history.jsonl
