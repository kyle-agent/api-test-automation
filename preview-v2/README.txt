Read-only PREVIEW of platform IA v2 (branch claude/zealous-heisenberg-irf3xt, unmerged).
Static — no FastAPI. index.html = Dashboard (coverage + API health, public main);
catalog/catalog.html = per-service catalog (✅/🟡/❌ validation badges + 🧩 compose);
ops.html = live ops. Edits/run dispatch deep-link to the control plane (live server).
