# HB1 (CAMPAIGN-C3-100, 2026-07-04): database depth batch 1 — mysql + mariadb
# self-sufficient cluster subops-full lifecycles (create→wait→subops→delete).
# Heavy/billable per campaign schedule (owner-approved batches). Target: H 27.
# Lane verified quiet before push: owned==1(known-stuck log-group, muted) +
# no active workflow runs + persona local run concluded & account probed clean.
# HB1↔HB2 merge forbidden (DB cluster quota); parallel=2 (DB ≤3 rule).
action=run
mutations=true
destructive=true
heavy=true
crud_ids=mysql-cluster-subops-full,mariadb-cluster-subops-full
parallel=2
