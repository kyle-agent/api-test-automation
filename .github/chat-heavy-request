# HB3 (CAMPAIGN-C3-100, 2026-07-04): virtualserver depth + image writes + backup chain
# compute-virtualserver-full (55 steps, shared VPC adopt) + vs-image-write-coverage
# + gen-heavy-backup (self-sufficient VM closure, own VPC; enabled this push,
# heavy-gated; -restore stays disabled/leak-unsafe). Target: H 53 + W 3.
# Known-issue expectation: delete-server 400 DeleteImpossible (backend, 2x
# reproduced) — reconciler end-sweep reclaims. Lane gates verified: HB2 concluded
# 23:31Z + folded + swept, owned==1 muted known-stuck log-group only.
action=run
mutations=true
destructive=true
heavy=true
crud_ids=compute-virtualserver-full,vs-image-write-coverage,gen-heavy-backup
parallel=3
