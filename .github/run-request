# Wave G HEAVY validation run (2026-06-16) — TRUE payoff of the VPC saga.
# Root cause finally fixed in 7648dbc: provision_shared_vpc names were 21>20 chars
# (regrvpcshared{8hex}) -> SCP 400 -> no SCP_SHARED_VPC_ID -> every heavy adopter skipped.
# Now regrvpcsh{uniq}=17 / regrsubsh{uniq}=17 / regrsubshdb{uniq}=19 (all <=20) -> provision 2xx.
# Plus 6b87007 broadens reconciler reclaim prefix regrvpc->regr (catches the regrw5vpc leak) and
# this run also names it explicitly (sweep_extra_names) as belt-and-suspenders.
# Expected chain: pre-reclaim frees cap (incl. regrw5vpc leak) -> provision creates VPC+subnet AND
# exports SCP_SHARED_VPC_ID/SUBNET_ID -> conftest shared_vpc populated -> adopters ADOPT (not skip)
# -> heavy chains run: gen-heavy-aimlops (SKE cluster -> aimlops release POST), heavy-shared-dbaas
# (eventstreams/epas/postgresql cluster CREATE), VM-full + the new B/C id-bound reads -> first
# service-attributed 2xx promotes the held nodes docs->VALIDATED.
# Also validates this branch's coverage work landed in 03f4e03 (VM gap7->0, ai-ml, 5 LIGHT non-DB).
mutations=true
destructive=true
heavy=true
sweep_extra_names=regrw5vpc6a2d542e
