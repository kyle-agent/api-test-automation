# HEAVY rev 5: capture envelopes (static-nat / product) self-identify via the
# new capture-failure body print; soft lookups (quota-request/support) skip
# empty lists; probe retries with the registry-envelope unwrap.
# scr chain still EXCLUDED so the probe owns the NON_VISIBILITY quota slot.
mutations=true
destructive=true
heavy=true
crud_filter=gen- and not scr
docker_probe=true
