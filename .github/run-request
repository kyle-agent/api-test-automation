# request: PLATFORM LIVE VERIFICATION #3 — heavy window (DBaaS sub-ops + eventstreams guarded)
# Validates the docs-derived sub-op steps inside the mysql/postgresql heavy
# lifecycles (read-only/reversible sub-ops while the cluster is live) and the
# corrected eventstreams guarded bodies. Billable: 2 DB clusters (~40min).
suite=full
heavy=true
service=eventstreams
crud_filter=database-mysql-cluster or database-postgresql-cluster or eventstreams-cluster-subops-guarded
