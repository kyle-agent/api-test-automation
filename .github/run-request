# request: VALIDATION run for the new parallel design —
#   slowest-first xdist ordering + DB-lane shared subnet (subnet#db).
#   Target: ADOPT pass wall-clock ~= max(slowest DB ~40-50min).
#   Scope: the 5 slow heavy lifecycles only; sweep_force cleans up after
#   (account confirmed idle — no mutating run live).
mutations=true
destructive=true
heavy=true
sweep_force=true
category=database
crud_filter=database-mysql-cluster or database-postgresql-cluster or heavy-shared-dbaas or container-ske-cluster-nodepool or compute-virtualserver-full
