# Wave F LIGHT run (2026-06-16) — land accumulated UI/coverage fixes (no heavy; held DBaaS deferred).
# heavy=false: smoke + read-chains + LIGHT CRUD only. Purpose = make these branch fixes live:
#  - read_chains required-query (getqueueattributes etc. now fire ?attributes=All -> 2xx, 400 noise gone)
#  - reachability waiver class in build.py -> cov_c3 recovers (reachability-covered counted, honest split)
#  - mobile-responsive per-service table (build.py .tbl-scroll + break-word)
#  - publish (build.py) regenerates dashboard with all of the above.
# held DBaaS real-validation (eventstreams/epas/pg) is DEFERRED (provision env-handoff still unresolved;
# not worth more heavy runs — diminishing returns). VPC-light.
mutations=true
destructive=true
heavy=false
