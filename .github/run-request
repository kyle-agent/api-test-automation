# mysql/mariadb replica family-only — cluster as clean prereq (no disruptive
# setter verifies). Setter ops already banked last run. Should land replica
# create/sync/reset/promote like PG/epas.
mutations=true
destructive=true
heavy=true
crud_filter=gen-heavy-mysql-replica or gen-heavy-mariadb-replica
