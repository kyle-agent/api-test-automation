# Wave H HEAVY validation run (2026-06-16) — finish the payoff Wave G's timeout cut short.
# Wave G (run 27626396618) PROVED the VPC root-cause fix: shared-VPC provision 2xx'd (18:22→18:26)
# and the VPC-CRUD serial class PASSED (was failing in Wave F). BUT the broadened regr* reclaim
# prefix + IGNORE_TTL matched a months-deep leftover backlog, and the reconciler deletes serially
# with a 150s _wait_gone each, so the pre-run reclaim ran 3.5h and pushed ADOPT-class CRUD into the
# 5h job timeout -> CANCELLED before the heavy adopters (aimlops/DBaaS/VM-full) could validate.
# Two things changed since: (1) Wave G's 3.5h reclaim + post-run sweep CLEARED the backlog, so this
# reclaim is small/fast; (2) IB-053 (a122f4d) TIME-BOXES the pre-run reclaim at 30m so it can never
# starve the run again. Net: reclaim finishes fast -> provision 2xx (proven) -> ADOPT-class CRUD now
# has ~4h budget -> gen-heavy-aimlops (SKE cluster -> aimlops release POST), heavy-shared-dbaas
# (eventstreams/epas/postgresql cluster CREATE), VM-full + B/C id-bound reads RUN TO COMPLETION ->
# first service-attributed 2xx promotes the held nodes docs->VALIDATED. C2 call-coverage recovers.
mutations=true
destructive=true
heavy=true
