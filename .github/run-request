# SCR repo-create isolation test (2026-06-15) — gen-scr-repo-test. Create repo
# 'test' on the EXISTING owner registry sample-nayvugfp (registry_id 'nayvugfp')
# via POST /v1/repositories using the standard SCP access/secret (no console SCR
# auth key needed for the REST API). Answers: is repo-create still 403, or does
# it work with full-permission access/secret? Light, no VPC. Broad expect_status
# records the real status/body; list-registries first shows the true registry id.
mutations=true
destructive=true
heavy=false
crud_filter=gen-scr-repo-test
