# Wave E HEAVY validation run (2026-06-16) — payoff of the VPC saga.
# Full chain now fixed: IB-049 (xdist graceful-skip), IB-050 (pre-reclaim + concurrency + -n2),
# IB-047 (SCP_SWEEP_IGNORE_TTL clears unexpired leftovers -> provision wins), + the env-handoff
# fix (provision_shared_vpc post-create crash no longer eats SCP_SHARED_VPC_ID; workflow gates
# on a non-empty id). Expected: pre-reclaim frees cap -> provision creates VPC+subnet AND exports
# SCP_SHARED_VPC_ID/SUBNET_ID -> conftest shared_vpc populated -> adopters ADOPT (not skip) ->
# heavy-shared-dbaas runs -> eventstreams/epas/postgresql cluster CREATE executes -> first
# service-attributed 2xx on POST /v1/clusters -> next promotion unblocks them.
# If provision still yields no id, the new ::warning:: + shared-infra.log will say why.
mutations=true
destructive=true
heavy=true
