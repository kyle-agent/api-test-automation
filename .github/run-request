# PG replica dedicated heavy window — first live test of the replica family
# (gen-heavy-pg-replica, 27 steps): cluster v16 -> replica create -> sync-
# replica-state -> reset-replica -> promote (irreversible, last) -> teardown
# (replica deleted before origin). Owner-approved same-region replica flow
# (2026-06-13). Self-contained (own VPC + 2 PG clusters); ~30-50min billable.
mutations=true
destructive=true
heavy=true
crud_filter=gen-heavy-pg-replica
