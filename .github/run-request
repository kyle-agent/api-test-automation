# PG replica rev4 — replica block storage role_type now DATA (rev3 proved the
# settling-retry rides past InvalidServiceState). create -> sync-replica-state
# -> reset-replica -> promote should land now.
mutations=true
destructive=true
heavy=true
crud_filter=gen-heavy-pg-replica
