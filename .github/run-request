# HEAVY rev 6: static-nat flat capture ($.id) + product *_id fields + probe
# PUBLIC registry (visibility quota slot — no contention with the scr chain,
# which re-enters this run with its registry_id check-dup fix).
mutations=true
destructive=true
heavy=true
crud_filter=gen-
docker_probe=true
