# PG replica rev3 — replica create now retries 400 InvalidServiceState while
# the cluster settles after RUNNING (rev2 proved cluster+sg-rules; this should
# finally land create -> sync-replica-state -> reset-replica -> promote).
mutations=true
destructive=true
heavy=true
crud_filter=gen-heavy-pg-replica
