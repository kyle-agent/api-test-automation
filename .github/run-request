# mysql/mariadb replica re-run — state-sensitive verifies now retry [400,409]
# until the cluster returns to RUNNING (epas already PROVEN; PG too). This
# should clear the archive/maintenance verifies and reach the replica family.
mutations=true
destructive=true
heavy=true
crud_filter=gen-heavy-mysql-replica or gen-heavy-mariadb-replica
