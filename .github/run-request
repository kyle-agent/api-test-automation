# request: PLATFORM LIVE VERIFICATION #1 — first run through the platform stack
# (suite expansion, env-profile path, per-run snapshot upload, static /platform
# Pages export). Read-only smoke: no mutations, no sweep needed.
suite=smoke
