# SCR docker-auth probe (2026-06-15) — owner plan: create a registry via API
# (random-suffixed host), docker login + pull/push with the SCP access/secret,
# verdict, then delete. Answers: are the SCP keys themselves the SCR docker
# credential (PUSH-OK -> cloud-ml custom_registry needs no separate console key),
# or is a separate console auth key required (LOGIN-FAILED)? One-shot, self-cleaning.
docker_probe=true
mutations=true
destructive=true
heavy=false
