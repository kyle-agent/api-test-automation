# request: PLATFORM LIVE VERIFICATION #4 — combined light wave
# (1) HMAC wire-signing fix (SCP_SIGN_ENCODEURI default-on): full unscoped
#     smoke is the broad no-regression proof + unblocks the check-duplication
#     query-param 401 class (scr/devops/aimlops/queues/cloud-functions...).
# (2) networking wave body fixes: gslb (TCP health-check) + cdn (cache TTL).
suite=full
crud_filter=networking-gslb-service or networking-cdn-service
