# Wave A LIGHT validation run (2026-06-15) — first validation-loop cycle.
# heavy=false: smoke + read-chains + LIGHT CRUD only (NO billable VM/DB/heavy clusters).
# Purpose:
#  (1) exercise newly-wired id-bound GET probe-reads (apigw tree showstage/showusageplan/
#      listreports, vs servers/{id}/volumes, vpc tgw/peering routing-rules, dns hosted-zone)
#      -> raise cov_getid;
#  (2) produce IB-041 verified_endpoints_evidence.json (per-endpoint real 2xx) -> unlock
#      docs->VALIDATED promotion next cycle;
#  (3) validate light-service creates (servicewatch/iam/queue/etc).
# VPC 5-cap: light networking self-creates 1-2 VPCs (well under the cap).
mutations=true
destructive=true
heavy=false
