# Wave D HEAVY validation run (2026-06-16) — confirm the IB-047 pre-reclaim TTL-override fix.
# Now: pre-reclaim runs with SCP_SWEEP_IGNORE_TTL=true -> force-clears our UNEXPIRED leftover
# VPCs (the reconciler previously SKIPPED them) -> the 5-VPC cap is freed -> shared-VPC provision
# should WIN its slot -> SCP_SHARED_VPC_ID exported -> adopters adopt (not skip) -> held DBaaS
# creates (eventstreams/epas/postgresql) RUN -> service-attributed 2xx -> next promotion unblocks.
# Expected vs Wave C: create-vpc exceed-max-count skips ~0; provision succeeds; verified endpoints up.
# (Account has 0 standing VPCs, owner-confirmed; only OUR leftovers occupied the cap.)
mutations=true
destructive=true
heavy=true
