# request: FORCE cleanup before measurement run #5 — reap run #4 leftovers
# (vpc-peering VPCs that 409'd, anything TTL-protected). Account must be at
# 0 regr VPCs so provision-first + the 10 heavy/adopt lifecycles get slots.
destructive=true
sweep_force=true
category=management
service=servicewatch
