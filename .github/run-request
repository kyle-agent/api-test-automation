# Backup heavy window — agentless VM_IMAGE backup chain (gen-heavy-backup,
# never run live): VM closure -> backup-target lookup -> policy create/
# AVAILABLE -> 13 verify sub-ops -> manual full backup + re-poll -> teardown.
mutations=true
destructive=true
heavy=true
crud_filter=gen-heavy-backup
