# Tier 0 — LIGHT regression run: VALIDATE the platform create→show(조회) change.
# Confirms the engine auto-probe (probe_reads seeded from full capture context)
# + the _PARAM_ALIASES name-mismatch resolver actually fire the child-id GETs as
# real 2xx reads across services (apigw is the exemplar; VPC-free, light). Also
# re-runs with the archivestorage 401 fix landed. ZERO heavy/billable spend.
mutations=true
destructive=true
heavy=false
conformance=false
