# request: FULL HEAVY coverage run on main — validates the A||B job split
# (regression-vpc-crud in parallel) + optional-retry caps, and gives the
# fail_new triage a fresh baseline. Account assumed idle.
mutations=true
destructive=true
heavy=true
