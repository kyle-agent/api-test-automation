# Heavy live VALIDATION wave (2026-06-15) — advance the VALIDATED node count.
# Full CRUD lifecycle run on current main (now includes: graph transitive-reduction
# fix, IB-022 source wiring, model improvements, AUTONOMOUS-LOOP controller doc).
# VPC 5-cap is respected by the existing shared-VPC adopt (conftest shared_vpc ->
# ~1 shared VPC for the 6 heavy chains) + regression/scenarios/dependencies.json
# vpc_schedule + core.budgets reservation (quota-exhaustion SKIPS, never fails).
# Goal: promote docs-provenance nodes to VALIDATED from real 2xx; feed fail_new to
# the Problem-Finder track. The publish step also restores the dashboard index
# entry-link (build.py on main now emits catalog/platform links).
mutations=true
destructive=true
heavy=true
