# PG replica re-run (rev 2) — setsecuritygrouprules body fixed (non-empty
# add_ip_addresses); the cluster spine already proved live (run 27468351624).
# This run should finally exercise the replica family: create -> sync-
# replica-state -> reset-replica -> promote -> teardown (replica before origin).
mutations=true
destructive=true
heavy=true
crud_filter=gen-heavy-pg-replica
