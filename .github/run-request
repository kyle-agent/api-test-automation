# Publish-only run after the platform-IA merge (main @ 6e44383).
# Purpose: republish the dashboard (now linking the 🧩 콘솔 /platform) + publish
# the new static /platform console (build_console.py) with THIS run's results.
#
# READ-ONLY SMOKE — explicitly NO mutations / destructive / heavy. Cheap (~15min),
# no resource changes. Cumulative coverage (verified_endpoints/endpoint_status) is
# merge-preserved, so the public dashboard numbers don't regress.
mutations=false
destructive=false
heavy=false
conformance=true
