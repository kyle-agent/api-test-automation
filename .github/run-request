# Publish-only run after the catalog-consolidation merge (#2).
# Purpose: republish so the console Catalog tab becomes the single canonical
# catalog surface — dashboard per-service downlinks now deep-link into
# platform/console.html?service=<cat>/<svc>, and the standalone catalog/ static
# export is retired (graph_export kept, just not published).
#
# READ-ONLY SMOKE — explicitly NO mutations / destructive / heavy. Cheap (~15min),
# no resource changes. Cumulative coverage (verified_endpoints/endpoint_status) is
# merge-preserved, so the public dashboard numbers don't regress.
mutations=false
destructive=false
heavy=false
conformance=true
