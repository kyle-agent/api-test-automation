# Per-service CRUD lifecycle fragments. One file per service: <category>__<service>.json
# Owned by that service's agent during the coverage campaign. Merged by
# regression/scenarios/loader.py with the base scenarios.json. See agents/CAMPAIGN.md.
